# 第三包：步骤持久化与崩溃边界验证

状态：预先准备；**等待第一包 G1 验收后才开始实现**。先读 `AGENT_NODE_VALIDATION_PLAN.md`。本包可与 02 并行，但不自行集成其未验收的上下文代码。

## 1. 目标与范围

复用 01 的 Node 入口、真实沙箱、立即完成与图集成桥，验证 Harness StepPersistence 能记录什么、重启后能够可靠判定什么，以及 Anchor 最少还需要多少恢复代码。

目标不是 exactly-once，不实现任意 shell 的副作用推断、自动对账引擎、分布式执行、人工等待、生产 resume 替换或 Graph 状态机重构。

不确定动作默认不重放。允许原型返回明确的 `uncertain`，这是一种正确结果；静默重复副作用不是“最终成功”。

## 2. 所有权与共享契约

建议分支 `research/node-recovery`，从 G1 SHA 建立。拥有最小持久化/恢复模块、故障注入脚本、自己的测试和报告。共享适配器、公共请求/结果、依赖文件和图桥的修改由主集成管理。

选择一种本地 StepStore 即可，优先满足原子写入与检查需求的现成实现，不同时适配 File/SQLite/Mongo。固定已评估版本。数据库或记录位于 Agent 不可写的控制目录，不能放进业务工作区。

建议向主集成提出最小扩展：恢复入口接收不透明恢复引用，结果可表示 uncertain；引用如何校验和解析由 Node 所有。Graph 不接收框架消息或自行选择 Pydantic checkpoint。不提前改变生产 Graph schema。

不得复制 01 的执行器或维护第二套完成解析。通过验收接缝组合 StepPersistence；所需钩子不稳定时返回具体问题，不引入私有 API 后隐瞒。

## 3. 恢复策略

复用框架提供的步骤记录、快照及 effect ledger。记录逻辑节点执行 ID 与每次进程/框架执行 ID 的关联，避免重试覆盖之前记录。每次试验真实退出进程再重启，不能用重新实例化 Python 对象代替。

| 已知事实 | 本包默认行为 |
| --- | --- |
| 有已持久化调用，确认尚未进入工具执行 | 可执行一次；若证据不足，按 uncertain |
| started，但无可靠终态/结果 | uncertain，禁止自动重放 |
| effect 已完成且结果与可继续快照完整 | 从该状态继续，不重复副作用 |
| 已知完成但结果/快照缺失 | 不重放；无法安全继续时返回 uncertain |
| 工具失败或超时 | 可能有部分副作用，不能仅凭 failed 判定可重试 |
| 检查点损坏、身份不匹配或引用无效 | 明确失败，不能偷偷开始一个全新任务 |

恢复重放授权不能通过猜命令“看起来只读”获得。测试若演示幂等/对账，只限明确知道语义的计数夹具；不得推广为任意 bash 自动修复。

保存并恢复已有请求预算，不因进程重启清零。对于已发出但未记录响应的模型请求，保守记账或明确不确定性，不能声称费用精确；真实费用核对不在本包内。摘要预算待 G2 与 02 合并验证。

## 4. 真实故障注入

使用隔离临时 workspace、真实 bubblewrap、确定性模型及真实 CLI。计数器命令在 sandbox 内原子更新 `counter.txt` 并写唯一 effect 标记。所有暂停点通过测试专用同步屏障触发，父进程在收到到位信号后 SIGKILL；不要靠 sleep 猜杀进程时刻。

明确主进程和沙箱子进程的关系。K3 要先确认命令已退出；另外增加运行中 shell 的试验，检查宿主死亡后子进程是否继续，父测试负责清理自己创建的进程。不能把“主进程死了”等同于“命令没有继续”。

| ID | 注入位置 | 重启后的验收 |
| --- | --- | --- |
| C1 / K1 | 模型调用已持久化，工具 started 之前 | counter=0；确有未执行证据时最多执行一次，否则明确 uncertain |
| C2 / K2 | started 已持久化，执行命令之前 | counter=0；系统仍须承认与 K3 不可仅靠 ledger 区分，不自动重放 |
| C3 / K3 | 副作用与命令返回之后，完成记录之前 | counter=1；uncertain；再次恢复不能变成 2 |
| C4 / K4 | 工具结果与可继续快照持久化后，Graph 完成记录前 | counter=1；可继续/结束且不重做；不能安全衔接则明确报告缺口 |
| C5 | effect 完成记录后、完整结果/快照之前（如存在此窗口） | 不凭 completed 盲目重放或假装已有结果；保留不确定性证据 |
| C6 | 同一恢复引用重复调用 | 不覆盖历史或重复确认的副作用；单进程顺序重试即可，不要求分布式锁 |
| C7 | 检查点损坏/错节点引用 | 有解释的失败，无命令执行或静默从头运行 |
| C8 | 多次中断、恢复到请求上限 | 累计预算不清零，无无限恢复循环 |
| C9 | shell 已启动，宿主被杀 | 记录子进程存活和最终 effect；无法证明停止则 uncertain；测试清理无泄漏 |
| C10 | 正常完成与混合图回归 | 01 即时提交、串行调用、真实 Agent→op→Agent 和 mini 默认路径保持 |

C4 应区分“最后工具为普通命令”与“最后工具为提交命令”，后者不得为了恢复再问一次模型。Graph finalize 接缝使用真实记录/Git，若无法安全幂等，报告限制，不在本包重写整个 scheduler。

研究包的 CRASH_RECOVERY_RUNBOOK.md 仅作输入，不是执行证据。每个窗口提供进程退出码、屏障确认、counter 前后值、ledger、快照、trace、工作区 Git 状态和实际模型/工具调用次数。

## 5. 接受何种结论

最低安全条件：不隐瞒不确定性，不自动重复未知副作用。只有确认完成且有足够恢复信息的路径实际继续成功，才能声称具备该路径的恢复能力。

若全部窗口都只返回 uncertain，属于安全探测结果，但未证明持久化足以支撑可用恢复，应报告“验证不满足迁移目标”。若框架缺少可靠步骤边界，使 K1/K4 无法成立，提交源码/API 证据和失败测试，不伪造检查点。

不得通过不执行 SIGKILL、stub 沙箱、去掉计数副作用或从头重新跑任务来通过验收。沙箱不支持则受阻，不算通过。

## 6. 交付

提交独立 commit、可重复故障脚本与 `AGENT_NODE_PLAN_03_RESULT.md`。写明 G1 SHA、精确依赖、本地 store、恢复引用格式、每个 C 项通过/失败/skip、所用公开或私有 API、剩余需自研代码。

提供单命令运行全部窗口的入口，有限超时、失败诊断和 finally 清理；无凭证、无真实外部副作用、无 provider 调用。按项目执行 pytest/ruff/mypy 并报告实际命令。

向 G2 交接：快照何时取得、历史如何恢复、ledger 与快照的非原子窗口、预算持久化口径、输出引用保留要求。03 本身不负责检验摘要内容或压缩策略。

不合并/推送主分支、不改生产 resume、不宣称 exactly-once。主验收 Agent 独立攻击 C2/C3/C5 的假设，再与 02 组合验收后决定是否具备迁移价值。
