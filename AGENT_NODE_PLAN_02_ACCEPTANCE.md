# 第二包主验收

结论：**结构性验收通过，允许进入后续组合验收；共享执行层仍需主集成复核。**

验收依据：`AGENT_NODE_PLAN_02_RESULT.md` 最新版、提交 `087ccff` 及其祖先修订。

## 已通过

- 第二包测试 51 项通过；全量 `tests/` 196 项通过，无 skip。
- `ruff check src/ tests/` 通过。
- `mypy src/anchor/` 通过。
- 上下文预算在发送前检查，摘要先于滑窗；超限重试会重新检查并单独记录实际发送。
- 追加记录与模型上下文分离；初始任务、指令、原始响应、工具参数、调用 ID、usage 和输出事件均可追溯。
- stdout/stderr 共用递减存储预算；输出采集不使用无界 `PIPE` 收集；超限、无存储和部分保存均显式标记 incomplete。
- 输出摘要命名稳定；相同内容去重；未超限流不会在挂载目录留下 raw 暂存文件。
- 摘要器调用、usage、失败状态均记录，且实例之间不共享状态。
- 实验判定器已加入精确文件集合、证据数量、逐字末行等机械检查；语义真实性保留 `human_review` 标记。

## 保留限制

1. `evidence` 有两项异常样本尚未人工裁定，不能据此宣称任务失败或成功。
2. mini 与 Pydantic 实验的上下文预算不完全对称，样本量仅三个任务、每任务两次，不能推断性能因果。
3. 摘要 usage 是框架响应 usage，不是 provider 账单级核验；摘要质量和引用来源真实性仍需人工评估。
4. 真实 provider 窗口拒绝仍由模拟异常覆盖；尚未声称 crash recovery、WAITING、人工输入或前端事件流已实现。
5. `src/anchor/runtime/sandbox.py` 与 `execenv.py` 的共享层改动通过本包测试，但仍需主集成复核后再纳入 G2。

因此，本包证明的是：在确定性测试和现有有限实验条件下，预算控制、压缩接线、追加记录和有界输出机制可工作；不证明两种执行器的性能优劣或生产级恢复能力。
