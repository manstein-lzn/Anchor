# Anchor Agent Harness 调研任务书

日期：2026-09-22。性质：外部调研委托，不是架构决策或实施授权。

## 任务目标

为 Anchor 选择 Agent Node 内部的执行底座。我们希望复用成熟实现，尽量不自行开发上下文压缩、工具执行生命周期和恢复机制，同时保持 Graph 与 Node 解耦。

请重点评估 PydanticAI，以当前 mini-swe-agent 为基线，必要时补充最多两个更适合的候选。不要预设必须迁移，也不要将“API 更多”视为更好。最终回答：哪种方案最少需要 Anchor 自己维护代码，仍然能可靠完成长任务？

本任务中的 PydanticAI 指 Agent 框架，不是 Pydantic 数据校验库。对候选框架的能力描述均须调查验证，本任务书不预先认定它们具备任何能力。

## 项目背景与现状

Anchor 是 Python 项目，前端为 React。Graph 定义节点、依赖、路由和循环；运行时的节点有两类：Agent 是模型循环，op 是无模型命令。模块图在执行前展开。当前节点按顺序调度，尚无运行中等待用户输入再继续的完整协议。

每个节点有独立工作区，跨图循环轮次保留。Anchor 在节点执行后创建 Git commit。边传递上游产物的固定 commit 引用，输入只读挂载在 `/in/<node>`，当前节点写入 `/workspace`。这些机制已经工作，本次不重做 Graph、工作区或 Git 模型。

Agent 当前使用 mini-swe-agent；最近本地检查版本为 2.4.6，依赖声明为 `>=2.4,<3`。模型仅看到一个名为 `bash` 的工具，Anchor 实际通过 bubblewrap 沙箱执行 `sh -c`。读写文件、运行程序、文献搜索均走 shell。文献能力由 `anchor-scholarly` CLI 提供。完成和路由通过 `anchor-done` / `anchor-route` 的输出协议识别，纯文字回答不能提交节点。

当前上下文行为：

- 初始提示包含角色指令、节点附加指令、公共规则、任务目标、直接上游摘要及文件索引。
- 更早的上游提供只读访问路径，模型按需读取；追溯在回边处停止，避免随循环次数无限展开。
- 一轮执行中累积模型与工具消息；没有自动 token 预算管理、历史压缩或上下文溢出恢复。
- 单次命令输出有字节截断，文献 CLI 支持分页；这不等于累计上下文受控。
- 节点新一轮执行使用新对话，复用工作区；中断恢复则从 trace 加载原对话。
- Anchor 自行扩展了 trace、恢复、沙箱及完成协议；恢复不是已经验证过的任意崩溃点恰好执行一次保证。
- 公共提示词混入了研究角色与文献工具要求，部分旧描述与现行挂载方式矛盾，需要拆清边界。

有仓库访问权限时，先读 `README.md`、`OPEN.md`，必要时读 `DECISIONS.md` 的 ADR-056 及以后。更早的架构文档可能描述已经删除的设计，不得当作当前实现。

关键代码：

| 路径 | 阅读重点 |
| --- | --- |
| `src/anchor/simple/agent.py` | `build_agent`、`TracingAgent`、`SandboxEnvironment`、公共 prompt |
| `src/anchor/simple/run.py` | `_task`、`_handed`、`_agent_for`、执行与恢复调用点 |
| `src/anchor/simple/graph.py` | 角色、节点与 op 的配置边界 |
| `src/anchor/runtime/` | 沙箱与研究工具实现 |
| `src/anchor/scholarly/` | Agent 可使用的文献 CLI |
| `tests/test_loop_provider_free.py` | 仅替换模型的真实执行测试方式 |

没有仓库访问权限时，以以上说明作为基线，并列出需要项目方补证的事项，不要假装检查过代码。

## 已确认的设计偏好

1. Graph 与 Node 通过清晰 API 交互；Graph 不依赖框架消息类型、不解析对话历史、不负责压缩。API 可以先是进程内接口，无需引入 HTTP 服务。
2. 优先保留单一 bash 工具。框架应能接入现有沙箱，不要求把每个 CLI 改写成模型 function tool。
3. 公共执行规则、角色指令与可选能力说明分离。提示词中的“未列出工具”不能当作权限隔离。
4. 完整执行记录与模型实际收到的有限上下文应能分离。压缩不能破坏恢复、工具调用配对和关键任务约束。
5. 复用现有 Graph、工作区、Git、CLI 和前端。无需模型的 op 不应被迫依赖模型服务。
6. 优先减少自研与运维负担。新增数据库、工作流服务或外部执行服务必须说明不可替代的收益及成本。

以上是评估约束，不要求现在写定最终接口或建设通用插件平台。

## 必须核实的问题

### 1. 上下文管理：最高优先级

- 是否有开箱即用的上下文预算、自动压缩、摘要和超限恢复？还是只有历史处理回调，需要用户自行写算法？
- 如何估算输入 token、为输出留预算、适配不同模型窗口？provider 拒绝请求后能否恢复？
- 压缩是否保留 system 约束、工具调用与返回的配对、待执行动作、重要文件引用？如何检查事实丢失？
- 是否支持大输出落盘、返回有限预览、随后分页读取？完整结果的保留和清理由谁负责？
- 是否支持持久化压缩结果并恢复？频繁重摘要的调用成本、延迟和 prompt cache 影响是什么？
- 哪些是框架稳定能力，哪些是官方示例，哪些必须由 Anchor 实现？

### 2. bash 与能力管理

- 只暴露一个 bash 工具是否顺畅？能否复用现有执行环境，而不采用框架自带沙箱？
- 工具调用校验、异常反馈、超时、取消、重试、输出限制分别由谁负责？
- 自动重试会不会重复执行有副作用的命令？能否关闭或控制工具重试？
- 单次模型响应多个命令时，执行顺序、并发和部分失败语义是什么？
- 能否按角色组合 CLI 用法与指令，并让公共运行规则独立存在？是否真的需要工具注册框架？
- 如何控制网络、凭证与文件访问？明确模型能力描述和实际权限 enforcement 的区别。

### 3. 执行、交互和恢复

- 完成条件能否继续由显式命令控制？不发工具调用时能否纠正，而非直接判定成功？
- 是否提供增量事件，区分模型文本、工具开始/结束、等待、失败和完成？
- 是否支持停止正在执行的工具、取消模型请求、暂停与继续？边界和限制是什么？
- 用户追问或补充信息能否在明确的安全点加入上下文？等待用户输入能否持久化，而不是占住一个调用栈？
- 进程重启后能恢复到哪一步？对于“命令已生效但结果未持久化”的窗口，框架如何处理？不接受无条件的 exactly-once 声称。
- 步数、费用、时间限制在恢复后如何累计？预算退出和任务失败能否区分？

### 4. 集成与长期成本

- 最小 Graph–Node 请求、事件、结果和恢复标识应包含什么？哪些类型应属于 Anchor，哪些留在适配层？
- Node 返回路由意图、Graph 校验和调度的职责能否保持？
- 哪些现有代码可删除，哪些需要适配，哪些会新增？特别标明是否仍需复制框架主循环。
- 模型供应商兼容、测试替身、许可证、维护活跃度、API 稳定性、版本升级风险如何？
- 对每个关键能力列出额外依赖，区分框架本体、配套产品和第三方组件；注明是否必须使用托管服务。

## 候选范围与调查方法

必须比较：

1. **保留 mini-swe-agent**：核查当前版本和最新稳定版差异，评估使用成熟扩展补齐能力的可能性。
2. **PydanticAI**：重点核查真实的上下文管理与持久执行能力，不能把 Pydantic 的类型安全当作 harness 完整性。
3. **至多两个其他方案（可选）**：只有在它们明显减少上述关键能力的自研量时才纳入，避免泛泛框架排行榜。

先查官方文档、版本发布说明和源码，再做最小验证。记录调查日期、包版本、源码 tag/commit。对关键结论给出链接及对应 API/源码位置；issue 只能说明问题线索，不能单独证明最新版行为。

所有能力使用以下标签之一，允许组合：

- **内置**：所评估版本直接提供。
- **官方扩展**：需额外组件，列出依赖与运行条件。
- **仅有钩子**：框架提供插入点，策略需要自己写。
- **需自研**：缺少可直接复用的实现。
- **不支持 / 未验证**：明确区分，不能将未找到文档直接判为不支持。

## 最小验证场景

先完成文档和源码筛选，再对基线与最有希望的候选运行同一套小实验。没有执行环境或模型凭证时交付可运行脚本与未验证清单，不能用推测填写通过。

| 场景 | 操作 | 需要提交的证据 |
| --- | --- | --- |
| 单 bash 接入 | 通过现有或等价的本地执行适配器读文件、写产物、显式提交 | 模型工具定义、动作顺序、退出状态；是否动了 Graph |
| 大输出 | 命令产生超过上下文预算的内容，在末尾放置一个任务所需标记 | 预览与完整结果的去向，模型是否能按需找回末尾标记 |
| 长对话 | 使用小预算强制触发上下文处理，早期给出后续必须遵守的约束 | 压缩前后 token/消息数、约束保留、工具配对是否合法 |
| 窗口超限 | 模拟 provider 返回窗口超限，再补充真实 provider 验证（条件允许时） | 是终止、重试还是压缩恢复；额外调用次数 |
| 崩溃恢复 | 对计数文件执行有副作用的命令，在执行前、执行后记录前、记录后分别中断 | 重启后的计数、是否重复执行、无法确定时如何暴露状态 |
| 用户交互 | 执行到等待点、持久化、重启、加入用户补充并继续 | 等待/恢复协议与事件顺序，是否需要外部服务 |
| 完成与预算 | 模型只说“完成”、命令明确提交、达到预算分别测试 | 三者是否被正确区分，恢复后预算是否延续 |

可用确定性模型替身验证控制流；它不能证明真实模型的摘要质量。将控制流测试与真实模型效果分开报告。真实模型实验注明模型、预算、调用次数、费用（可取得时）及观察到的延迟，不以一次成功宣称普遍可靠。

实验应在独立目录或分支中完成，不修改 Anchor 主线，不提交架构 ADR，不替项目方作迁移决定。

## 交付格式

请提交一份中文报告 `AGENT_HARNESS_RESEARCH_REPORT.md`，以及必要的最小实验脚本、依赖锁定信息和脱敏结果。报告应能在没有聊天记录的情况下独立阅读。

报告按以下顺序组织：

1. **结论摘要**：推荐保留、迁移还是继续验证；理由、最大风险、结论置信度。允许“不迁移”。
2. **版本与证据范围**：调查日期、候选版本、源码版本、实际执行环境、哪些没有验证。
3. **能力矩阵**：逐项覆盖本任务的问题，每格注明能力标签、证据和 Anchor 仍需承担的工作。
4. **实验结果**：每个场景的预期、实际、复现命令、关键日志及失败原因。
5. **集成草图**：给出最小 Node API 的伪代码，以及 bash、上下文、记录和 Graph 的责任归属；不设计完整平台。
6. **迁移与维护成本**：可删除/修改/新增的模块，额外服务，粗粒度工作量估计及假设，升级风险。
7. **建议与待决问题**：近期最小可交付方案、将来再加的能力，以及最值得项目方确认的少量问题。

矩阵建议列：`能力 | mini 基线 | 候选方案 | 证据 | Anchor 自研内容 | 风险/限制`。

不要只给一个综合分数。上下文和恢复中的关键缺口，不能被供应商数量、文档质量等分数平均掉。

## 决策标准

优先考虑：关键场景的实际正确性、Anchor 必须维护的代码量、Graph–Node 边界是否清晰、部署与长期维护成本。单 bash、可接入现有沙箱以及可保留显式完成语义是集成的重要约束。

如果某框架只有历史处理钩子，而摘要策略、输出存储、检查点和崩溃恢复仍全部由 Anchor 开发，请直接说明“它改善了执行框架，但尚未解决减少 harness 自研的主要目标”。若配套组件能够补齐，也请给出完整组合的成本，而非仅介绍框架本体。

最终决策由项目方与主开发 Agent 在审阅证据后共同作出。
