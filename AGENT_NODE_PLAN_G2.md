# G2：Agent Node 上下文与恢复组合验证计划

状态：待实施。本文是交给实施 Agent 的执行任务，不是 G2 已通过的声明。

## 1. 目标与当前基线

在同一 Node 执行链中组合 01 的执行器、02 的上下文能力和 03 的步骤持久化，使用真实进程中断与重新执行，回答：这套 Harness 能否在有限上下文、有限请求预算下安全恢复并完成任务，以及 Anchor 还需要多少适配代码。

阅读顺序：`README.md`、`OPEN.md`、`AGENT_NODE_VALIDATION_PLAN.md`、本文；然后阅读 01–03 的 PLAN、RESULT 与 ACCEPTANCE。架构争议再查 `DECISIONS.md`。以实际源码和可复现证据为准，不把报告结论当作测试。

- G1 冻结基线：`b9aae105d2ab0beb24afa93272a9d0186e4aa70f`。
- 当前候选包含 02 的 `087ccff` 与 03 的 `919b70b`。开工记录实际 HEAD、工作区状态和依赖精确版本，不倒退或覆盖用户改动。
- 02 结构性验证已有进展，共享 `sandbox.py` / `execenv.py` 仍需主集成检查。
- **03 上轮“通过”结论需要收窄。** 其验收文档里“已证明可继续”和“C5 窗口不存在”等说法不能作为本包前提。先过下文 A 门槛。
- 现有测试数只作基线，不是通过条件；新增测试必须能抓住具体错误。

默认不调用付费 provider，不读取或输出凭证。确定性模型验证机制；摘要语义质量和真实费用保留为未验证。

## 2. 架构边界与所有权

Graph 只交付 Node 请求，接收状态、结果和不透明恢复引用。模型消息、压缩策略、步骤 store、快照选择和恢复判断均留在 Node 内部。不得让 Graph 解析框架检查点。

复用 `run_node`、`build_agent`、`read_completion`、`NodeSandbox`、`context_capabilities` 和现有 recovery 模块；不得复制执行器或另写完成协议。模型工具集仍恰好只有 bash。保留 mini 默认路径。

本包由一个主集成实施者拥有适配器、公共契约、共享执行层、图桥和集成测试。可为实际恢复补最小 API 接缝，说明请求、结果、错误与预算的归属；不要预建插件总线、多 store 抽象或第二套 scheduler。不得引入私有 API 作为生产依赖；测试注入可包装公开 store 方法，但须明确这是故障测试设施。

范围外：默认执行器切换、生产 resume 替换、任意 shell 的自动对账、exactly-once、分布式锁、WAITING/人工输入、前端、实时流、工具市场。遇到框架限制允许得出“不迁移”，不得扩大范围把验证变成重写框架。

## 3. A 门槛：补齐第三包独立证据

先逐条补验；A 未满足前，不宣称恢复能力已成立。B 的夹具和只读分析可提前准备，依赖 A 的实现按顺序进行。

| ID | 当前证据缺口 | 要执行的补验与通过条件 |
| --- | --- | --- |
| A1 实际继续 | C4 检查 verdict/快照；`continued_messages` 仅取回历史 | 进程一完成副作用后 SIGKILL；进程二加载检查点，用同一 Node 执行实现继续到有效提交。计数器仍为 1，剩余动作确实执行，模型和工具调用数可查。只读取 messages 不算恢复成功。 |
| A2 写入间隙 | C5 在后续钩子看到 ledger 和快照同时存在，不能证明两次写入原子 | 核对固定版本 StepPersistence/FileStepStore 的实际写入顺序，记录源码位置。若独立写入，在真实 store 第一项持久化返回后、后一项写入前设屏障并 kill。必须覆盖已有旧 complete 快照时的新一轮中断：不能因“存在某个 complete 快照”就恢复并重复新副作用。若声称原子，提供覆盖两项数据的同一事务/原子提交证据。 |
| A3 重复恢复 | C6 只是重复 assess | 顺序两次调用真实恢复入口，分别覆盖 uncertain 引用、可继续引用和已完成引用。未知动作不执行；已确认动作和提交不重复；原 run 历史不覆盖。无需支持并发恢复。 |
| A4 引用与损坏 | 当前样例不足以证明身份绑定及检查点损坏处理 | 使用合法编码但 node/store/run 不匹配的引用，配合实际损坏的事件、快照和预算文件。拒绝时零模型/工具调用，不静默从头开始。验证版本、字段类型、负预算、未知 run；引用预算不能覆盖控制目录里更高的已用预算。仅能 base64 解码不代表身份有效，无需为本地可信调用增加凭证系统。 |
| A5 真预算累计 | C8 手动 save_budget(3)、save_budget(6)，未发生请求或恢复 | 真实请求后中断、重启，至少两次中断直至预算耗尽。已发请求未收到响应也保守占额或显式记录不确定消耗；达到上限后无新请求。用模型接收端独立计数与持久化预算对照，不手工设置预期累计数。 |
| A6 提交与图完成间隙 | Node completed 会使框架 run 被取消；正常完成钩子不保证执行 | 分开普通 bash 和 `anchor-done`/路由提交。有效提交已持久化但 Graph 尚未 finalize 时 kill，恢复不得再问模型或重跑提交。使用真实 Git/图记录验证；无法安全衔接则明确标为阻塞，不能仅用普通命令恢复代替。 |

修正 `AGENT_NODE_PLAN_03_RESULT.md` 中被反例推翻的结论，保留历史证据并注明适用 commit。主验收 ACCEPTANCE 由审阅者更新，实施者不要自行签署通过。

## 4. 共享执行层检查

在组合前核查 02 引入的输出采集与挂载变更：

- stdout/stderr 的模型预览和共享存储额度是不同限制。并发双流不能丢失本应可见的短输出，也不能无提示地少返回内容。
- 临时文件、持久输出、去重与总预算一致；运行期间内存/磁盘占用有界，不能只断言运行结束后的目录大小。
- 超时、异常、宿主被 kill 后的管道/线程/子进程清理有有限截止时间；无 spill 配置不返回已删除路径。
- 恢复后只读挂载真正可读，节点不能写审计记录、预算、步骤 store；验证真实 sandbox 内的读写结果。

发现共享层问题在其源头修复，并保留最小反例。不得以旧测试已通过替代这次检查。

## 5. B 门槛：上下文与持久化组合矩阵

在同一 Node 的真实执行中同时开启上下文策略、原始记录、StepPersistence 与预算管理。记录 capability 顺序及各钩子保存的具体历史，避免分别测完后把结果拼成组合证据。

| ID | 场景 | 必须观察到的结果 |
| --- | --- | --- |
| B1 压缩后恢复 | 真实触发摘要/压缩，检查点落盘后 kill，另进程继续到提交 | 恢复后模型实际收到有限上下文，包含必要约束和阶段成果；不会恢复成未压缩的全部历史。原始记录仍可查回被压缩掉的内容。 |
| B2 压缩两侧中断 | 分别在压缩开始前、压缩完成但对应检查点未提交、检查点已持久化时 kill | 恢复选择一致的历史版本；不会拼接不匹配的摘要与工具结果。不能安全继续则明确 uncertain，不能冒充 B1 成功。 |
| B3 副作用间隙 | 先压缩，再在 started 后执行前、副作用完成后终态前、终态后快照前 kill | 前两类保持 uncertain，不自动重放；最后一类依 A2 的真实边界验证。已有旧快照不得掩盖之后的副作用。 |
| B4 输出引用 | 大输出、中等输出、部分保存/达到额度后中断并恢复 | 恢复进程中的真实 bash 可读已保存输出的唯一末尾标记；部分输出明确 incomplete。模型不能修改输出和内部记录。 |
| B5 预算组合 | 主模型、摘要、窗口溢出重试跨多次恢复 | 单独记录各类调用并共用声明的总额度；重启、重摘要、retry 不清零。发送前拒绝不记作已发送；已发送无响应保守占额。预算耗尽有限终止。 |
| B6 原始记录关联 | 多次压缩与恢复 | 初始输入、原始模型响应、完整工具参数、tool_call_id、结果、摘要与快照可按逻辑 node、attempt/run 和历史版本关联；不覆盖旧记录，不以最终 trace 代替追加记录。 |
| B7 完成控制流 | 单响应多工具调用，中途有效提交，随后还有命令；在提交附近中断 | 串行执行，提交后命令永不执行；恢复不额外请求模型、不重复提交，route 与完成状态符合 01 契约。 |
| B8 图闭环 | 真实 Agent→op→Agent 中一个 Agent 压缩后中断恢复 | 图能继续到预期结束，产物、Git 和图完成记录一致；重复恢复不生成重复完成动作。非 Agent op 不依赖 Harness 内部对象；mini 回归通过。 |

大输出引用的保留期必须覆盖所有仍可恢复的检查点；本包不实现自动 GC。明确清理责任与何时允许删除，不能为通过测试把整个宿主控制目录开放给模型。

## 6. 故障实验与断言规则

1. 使用隔离临时 workspace、真实 bubblewrap、真实 CLI、真实 Git；不访问外部服务。记录精确 Python/框架/沙箱版本。
2. 所有 kill 窗口通过屏障同步，不 sleep 猜时刻；父进程确认到位再 SIGKILL，记录退出码。恢复必须是新的操作系统进程。
3. 使用计数器和唯一 effect 标记观察副作用；夹具的幂等性不能掩盖重复执行，工具进入次数也须独立记录。
4. 模型替身以可跨进程核对的阶段/调用证据驱动，不能因压缩改变历史长度而偷偷重置脚本或伪造剩余任务。保存每次实际收到的请求供断言。
5. 摘要替身只对输入中存在的约束给出确定性摘要；断言输入含该约束、摘要进入恢复后的实际请求。不得硬编码正确答案掩盖上下文丢失。
6. 对账本、快照、预算和历史建立必要的绑定检查；仅“有 complete 快照”不足以证明覆盖所有已完成 effect。
7. 每条实验有限超时，finally 清理自己创建的进程。清理只针对已跟踪的进程/进程组，避免宽泛字符串匹配误杀其他任务。
8. 缺少 sandbox、屏障没到、恢复没运行、断言失败均非通过。脚本对失败/受阻返回非零退出码，单选案例自动准备依赖或明确报错。
9. 普通进程 SIGKILL 的结果不等价于断电耐久性；rename 的原子可见性不等价于多个文件的事务。报告明确测试故障模型。

每个案例保留：基线 SHA、case/逻辑 node/attempt/run ID、屏障位置、kill/恢复进程退出码、counter 前后值、工具/模型/摘要调用数、预算轨迹、ledger/快照引用、原始记录位置、Git 状态、恢复 outcome 与最终断言。检查历史不可变可用文件摘要，不需要再造证据数据库。

## 7. 执行顺序与交付

建议独立分支 `research/node-g2`，不自动合并或推送主分支。

1. 记录环境，复跑现有 01–03 测试，建立可复验基线。
2. 补齐 A1–A6 与共享层检查，修复错误结论；若核心恢复路径无法成立，交付阻塞证据，不强行进入组合成功路径。
3. 接通最小恢复入口与能力组合，执行 B1–B8。共享文件由同一实施者顺序修改；不要把本包拆成几个同时改适配器的任务。
4. 完成正常路径和 mini 回归，提交结果文档及独立 commits；主验收独立重跑并攻击旧快照、预算耗尽与重复提交场景。

优先扩展现有 `scripts/recovery_windows.py` 与现有测试；组合断言可集中在 `tests/test_node_integration.py`。如需另起脚本，必须复用现有执行实现。报告提供一条运行全部 G2 案例的命令及每个案例单独重现的方法。

最低检查命令：

```bash
.venv/bin/python -m pytest tests/ -q
.venv/bin/ruff check src/ tests/ scripts/recovery_windows.py scripts/context_experiment.py
.venv/bin/mypy src/anchor/
```

新增脚本也加入静态检查。记录命令、退出码和测试数量；旧问题与本包引入的问题分开报告，不无关扩改。

实施方交付 `AGENT_NODE_G2_RESULT.md`，包含：

- A/B 每项通过、失败或受阻及证据路径；真实执行与仅静态分析分开。
- API 改动、共享文件清单、各修复 commit、固定依赖版本。
- 快照覆盖的历史/effect 范围、恢复判断、终态提交、预算和输出生命周期说明。
- 已发现框架限制、未验证事项及剩余自研量；不再引用被推翻的 03 结论。
- 可重复故障入口与上述验证结果。

主验收方最终写 `AGENT_NODE_VALIDATION_RESULT.md`，汇总独立验收结论。实施方不自行将测试通过提升为迁移批准。

## 8. G2 通过标准与迁移决策

安全底线：未知副作用不自动重放；不能误用旧快照、重置预算、重复提交、丢失原始证据或把损坏引用当新任务。

可用性底线：至少已 settle 的普通工具路径、压缩后恢复路径、已持久化有效提交路径都实际继续/结束成功；Graph 闭环能衔接。全部返回 uncertain 只说明保守，不满足迁移目标。缺少其中路径应明确部分通过/阻塞，不使用其他测试数量抵消。

满足结构性条件后，才另立 ADR 和正式迁移范围，评估正式接入、有限接入或暂缓迁移。G2 本身不切换默认执行器，也不证明真实模型摘要质量或生产长期可靠性。
