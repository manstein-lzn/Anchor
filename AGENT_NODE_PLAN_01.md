# 第一包：PydanticAI Node 最小适配与控制流验证

状态：交给外部实施 Agent 的计划；不是正式迁移决策。

基线：`52c94bfbef956ebf9724b30d1e7c9f646866d709`。执行前记录实际 HEAD 与工作区状态；保留用户已有改动。研究文档当前可能尚未提交，创建独立工作区时需显式带上这些资料。

## 1. 目标与完成边界

在保留 mini 默认执行路径的前提下，提供一个可被独立调用的 PydanticAI Agent Node 原型，用确定性模型驱动 **真实 Anchor 沙箱、真实完成 CLI、真实输入挂载**。

本包必须回答：PydanticAI 能否保持单 bash、串行执行、显式提交即停止、合法路由和权限边界，同时通过 Anchor 自有接口交付结果？后续上下文包和恢复包应能复用这个入口。

本包不实现：自动压缩、ToolOutputLimits、StepPersistence、崩溃恢复、WAITING/人工输入、前端事件流、生产配置切换、通用插件平台。不要复制框架执行循环，不接入第二个模型工具，不将 op 改成模型节点。

交付是可运行的集成原型与测试，不是只画接口；通过本包也不代表可以正式迁移或承诺长期任务可靠。

## 2. 先读什么

依次阅读 `README.md`、`OPEN.md`、`AGENT_HARNESS_RESEARCH.md`、研究报告。架构历史仅以 `DECISIONS.md` ADR-056 及以后为当前设计背景。

核对实际代码：

- `src/anchor/simple/agent.py`：沙箱、完成识别、公共规则、模型构造。
- `src/anchor/simple/run.py`：`_task`、`_handed`、`_agent_for`、结果记录和恢复。
- `src/anchor/runtime/sandbox.py`：真实执行、输出截断、超时与挂载。
- `src/anchor/done/__main__.py`、`src/anchor/route/__main__.py`：真实协议。
- `tests/test_loop_provider_free.py`、`tests/test_ops.py`、`tests/test_agent_resume.py`：已有行为与回归入口。

不要照搬研究 probe：它用 `ANCHOR_DONE` 子串判断完成，未使用真实沙箱，并在提交后再请求模型。它只证明了基本接口可用。

## 3. 实施边界

建议独立分支 `research/node-controlflow`，一个实施负责人即可，不拆成多个同时修改契约的任务。

允许新增 `src/anchor/node/` 下最少量的契约与 Pydantic 适配代码、对应测试、可复现执行说明。允许在 `pyproject.toml` 增加可选验证依赖，或者提供单独的精确版本验证 requirements；不要替换默认运行依赖。PydanticAI 固定 `2.46.0`，若直接使用 Harness 则固定 `0.32.0`，导入仅限适配层。

现有 `simple/agent.py` 与沙箱辅助代码只允许做必要的复用抽取，必须保留 mini 行为并补回归验证。不要重写 Graph scheduler、图 schema、Git 提交模型、服务 API 或前端。暂不改正式 ADR 宣告迁移。

本包允许实验测试通过现有执行工厂接缝注入适配器，证明一次真实图运行；不要求现在让生产调度器全部改用新 API。兼容桥属于实验边界，不能让新 Node API 暴露 `.env` 或框架 messages。

## 4. 对外契约：冻结语义，不冻结内部类结构

提供一个异步 Node 执行入口，输入和输出使用 Anchor 自有数据类型。可采用普通 dataclass，无需注册器、抽象工厂或序列化框架。

| 输入 | 语义 |
| --- | --- |
| 执行标识 | 区分一次节点执行；不假装已支持跨进程恢复 |
| task / instructions | 任务与角色要求；公共执行规则由 Node 侧提供 |
| workspace | 已由调用方准备的节点工作区 |
| inputs | 已解析的主机路径与只读挂载路径；Node 不遍历 Graph |
| routes | 允许选择的目标 ID；Node 可校验，Graph 保留最终调度权 |
| network / timeout / request limit | 明确的执行限制，测试使用小值 |
| trace path | 位于工作区之外，不能作为节点产物暴露 |

模型构造及确定性替身注入留在适配器工厂，不放入通用请求对象。通用契约不包含 PydanticAI、mini 或 LiteLLM 类型。

输出至少包含 `status`、`submission`、`route`、`model_requests`、`trace_ref` 和失败原因。状态至少区分 `completed`、`budget_exhausted`、`failed`；非完成状态不产生可供调度的 route。这里的请求计数限定为本次执行，不宣称预算已跨恢复累计。

本包不添加假 resume token、占位 WAITING 或空事件接口。后续包需要扩展契约时，由主验收 Agent 统一收敛。

## 5. 必须保持的执行语义

### 单 bash 与真实执行

- 模型可见的工具集合严格为一个 `bash(command: str)`，没有隐藏的完成工具、读取工具或 output function。
- 每个动作走 Anchor 的 BubblewrapWorkspaceSandbox；禁止直接宿主机 `subprocess_shell` 替代。
- 同一响应内的工具按顺序执行，不并发；已执行命令不得被框架自动重试。模型下一轮显式选择再次调用是另一回事。
- 非零返回码与超时作为可辨认的观察返回模型，不能伪装完成；沙箱不可用作为执行失败暴露。
- 框架工具取消不等于子进程终止。本包复用并验证现有命令超时；主动取消与完整进程树治理不在本包完成声明内。

### 完成与路由

- 零个或一个出口：使用 `anchor-done --summary ...`，首个非空输出行精确匹配 `COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`，退出码为零才接受。
- 多个出口：`anchor-done` 不能提交，必须使用 `anchor-route --to <合法目标> --reason ...`；接受目标、摘要并立即结束。
- 普通文字“完成了”、子串匹配、输出中间出现标记都不能提交；非法目标反馈错误，允许下一轮纠正。
- 新适配器拒绝非零退出或超时携带的完成/路由标记。现有 mini 多出口分支未统一检查退出码，应在行为差异中明确记录；不要顺手改变旧路径或为了完全一致复制这个漏洞。
- 接受提交后，不再请求模型，不再启动同一响应剩余工具，返回摘要来自 CLI，不等待模型补一句最终回答。
- “立即结束”的边界是**当前 shell 命令返回并被宿主识别之后**。`anchor-done; touch x` 在同一个 shell 调用中仍可能执行 `touch`，本包不假装 CLI 可以中断整个 shell。协议要求完成命令放在当前命令末尾。
- 标记是现有输出协议，不是不可伪造的身份认证。不要声称证明了标记一定来自 CLI 二进制。

优先使用 PydanticAI 的公开运行控制 API 实现终止。仅设置 completion flag + output validator 不足以验收。若必须使用私有 API、复制循环或新增模型工具才能做到，应提交可复现限制和最小替代方案，不悄悄放宽语义。

### Prompt 与记录

公共 prompt 只描述工作区、只读输入、工具行为、完成协议。研究角色和 `anchor-scholarly` 使用要求通过显式指令传入，不再强加给每个节点。文件交付与摘要作用不得使用旧模板中的矛盾描述。

记录模型响应、工具执行结果、调用顺序和终止原因，用于验收。可以包含适配器私有的原始框架数据，但只向调用方暴露 trace 引用。不要把原型 trace 接入生产 UI，不宣称已有 crash checkpoint 或自动恢复；正常终止后的工具配对与未执行调用处置必须可解释。

## 6. 验收矩阵

用确定性模型只替换模型响应。其余涉及沙箱的测试使用真实 CLI、挂载、文件、Git。所有模型调用均禁止实际 provider 请求。

| ID | 场景 | 必须自动断言 |
| --- | --- | --- |
| A1 | 单 bash 写文件并提交 | 工具列表恰为 bash；真实产物内容；摘要；完成状态；模型请求数 |
| A2 | 先回复普通文本，后正确提交 | 纯文本不成功；收到纠正后可以继续；纯文本重复响应有有限失败边界 |
| A3 | 提交即终点 | 模型在提交后若再次被调用就主动抛错；正常执行仍成功 |
| A4 | 同响应多工具：写 before、提交、写 after | before 存在，after 不存在；工具顺序确定；无额外模型请求 |
| A5 | 多出口，先 done 后合法 route | done 不完成；route 完成；目标和摘要正确 |
| A6 | 非法目标后纠正 | 未知目标不调度；错误可见；后续合法提交成功 |
| A7 | 假完成 | 子串、非首行、非零退出、超时携带标记均不成功；路由也覆盖非零退出 |
| A8 | 普通命令失败及超时 | 可见返回码/超时信息；不自动重做副作用；后续允许模型纠正 |
| A9 | 真实权限边界 | 自身 workspace 可写；`/in` 和自身 `.git` 写入失败且内容不变；network=false 使用现有网络隔离探针验证，不依赖公网连通性 |
| A10 | 请求预算耗尽 | 没有提交时返回 budget_exhausted 或明确配置的格式失败；不误报完成，不多请求一次模型 |
| A11 | 两次节点执行隔离 | workspace 产物按设计保留；completion/route/消息不会从前一实例串入后一实例 |
| A12 | 真实 Graph 小闭环 | 通过实验适配接缝运行含 Agent→op→Agent 的图；真实输入 commit、只读挂载、产物与最终状态正确；op 不调用模型 |
| A13 | 默认路径回归 | mini 原有控制流、循环、op、resume 测试继续通过；不安装可选 Pydantic 依赖也能导入和使用默认路径 |

A4 必须观察实际副作用，不能只检查“工具声明 sequential=True”。A8 可以在一次调用内先写计数文件再失败，用计数证明没有框架级重试。

A12 可用测试专用桥接把新结果映射给现有调度器。不得 stub 掉 Git freeze、输入视图、沙箱或 op 来宣称端到端通过。图路由到真实分支可在该场景或单独小图中补充；直接返回 route 字符串不等于调度验证。

## 7. 执行顺序与停止条件

1. 记录基线、运行相关旧测试，检查 bubblewrap 可用性。隔离安装精确候选版本。
2. 完成最小入口、真实沙箱适配与 A1–A4。首先解决立即终止，不要先写一大套抽象。
3. 补完成/路由负例、预算、权限和隔离测试。必要的共享抽取保持小 diff。
4. 完成 A12 真实图集成与默认路径回归。
5. 整理契约、运行命令、实际日志和未验证事项，提交可审阅 commit。

若环境不支持真实沙箱，测试可以标记 skip，但本包验收状态必须为“受阻/未验证”，不能写全部通过。不得通过扩大权限、跳过负例或吞异常来消除失败。

若公开框架 API 无法满足提交后停止，或适配必须大改 Graph，请停在具体失败证据处交回主验收 Agent；这也是本轮验证有价值的结论。普通实现错误、依赖安装和测试定位由实施方自行解决。

## 8. 交付与验收

提交代码和 `AGENT_NODE_PLAN_01_RESULT.md`，报告至少包含：

- 基线 SHA、实施分支与 commit SHA、修改文件和依赖版本。
- Node 请求/结果的最终字段与调用示例；公开/私有框架 API 使用情况。
- 安装与测试的完整可复制命令，Python/OS/bubblewrap 信息；实际通过、失败、skip 数量。
- A1–A13 对应测试名称、结果与关键证据。附工具顺序、请求计数、产物/commit 检查结果，不只给 pytest 总数。
- 默认 mini 与新路径的行为差异，尤其退出码、失败、完成和 trace。
- 后续上下文包、恢复包应复用的入口与测试夹具，尚未实现的边界。

沿用项目 pytest、ruff 和 mypy 检查；至少运行新增包测试及既有循环、op、resume 回归，再运行完整后端测试。类型检查覆盖新增模块。文档中列出实际命令，不要求执行方猜测。

交付 commit 应只包含本包改动，不合并主分支、不 push 到主分支、不代写正式迁移 ADR。保留研究原件。不要提交虚拟环境、凭证、巨量依赖或临时产物。

主验收 Agent 将审查差异并在本地重跑关键测试。只有完成语义、真实沙箱和图集成均通过，才冻结此入口供第二、三包并行使用。工作包完成不等于正式迁移完成。
