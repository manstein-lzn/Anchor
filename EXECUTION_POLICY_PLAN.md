# 进展驱动执行与恢复：开发及验收计划

日期：2026-09-08，复核 2026-09-09。状态：**阶段 1–3 已实现并验收；阶段 4 部分完成；
阶段 5 部分完成；真实联网调研验收已通过**。
本文件保留为原始计划与验收依据，实现事实以代码、迁移与测试为准。

## 实现状态（2026-09-09 复核）

| 阶段 | 状态 | 证据 |
| --- | --- | --- |
| 1 策略与状态契约 | 完成 | 限制清单 `docs/limits.md`；`ProgressEvidence`/`DiagnosticRequest` 在 `domain/models.py`；观察状态与诊断在 `watchdog.py`；故障分类 `worker.classify_failure`；`tests/test_execution_reliability.py` + `tests/test_watchdog.py`（注：早期 `runtime/execution_policy.py` 在架构精简中删除，只保留有消费者的部分） |
| 2 解除默认限制、拆分重试 | 完成 | 移除 `max_rounds`/`run_timeout_seconds` 隐藏默认；`expire_run_budgets` 默认 false；`output_retries` 默认 0；migration `0014_recovery_schedule` 持久化 `last_error_class` + `next_attempt_at`，尊重 Retry-After，重启不丢恢复计划 |
| 3 进展观察与诊断 | 完成（规则层） | `watchdog.py` 写 `progress_evidence`、去重 `diagnostic_requests`；`supervisor_service.py` 接入；migration `0013_progress_evidence`；`tests/test_watchdog.py` 7 passed |
| 4 API 与前端说明 | 部分 | 新增 progress/diagnostics API 与 Run Console 两个区域；未新增独立“业务轮数 vs 故障重试”分组表 |
| 5 迁移与部署交接 | 部分 | 学术示例移除隐藏上限；README/STATUS/API/WEB/DECISIONS(ADR-019/021/022)/HANDOVER 已更新；未产出独立迁移报告文件与发布/回退 runbook |

### 真实联网调研验收（2026-09-09）

使用 Pi 的 DeepSeek `deepseek-v4.1-flash-expires-on-0910` 跑通 academic-research v3：
Run `39d5db63-a35b-42d3-9510-058fcc3a3c5b` COMPLETED，2 轮修订（review a0=revise →
a1=pass），158 事件，约 25 分钟；最终 Markdown 报告 29,502 字，
`.local/artifacts/reports/<run_id>/report.md`（48,080 字节），含 References 与检索溯源。
检索为真实 Crossref/arXiv 调用（16 次检索、28 项来源、11 篇全文阅读）。
尚未做的只剩「独立审查者」一项。

### 仍未实现（诚实边界）

- 自动修复动作：`DefaultDiagnosisPermission` 只允许 continue / wait / escalate；无 repair/reconcile 授权。
- 校准过的自适应检测器：当前是规则 + 持久化观察，未做误报/漏报校准。
- 阶段 4 的三类计数分栏展示、阶段 5 的独立迁移报告文件。
- 诊断模型本身尚无执行器；诊断是持久化请求 + 操作员动作，不是自动 LLM 诊断。

## 目标与范围

恢复 README 开发原则 8、DECISIONS 的 ADR-000C / ADR-017、HANDOVER 的不可破坏约束：
健康 Agent 不因预设研究轮数、累计工具调用数或任务时长而终止。
完成由可验证的结果决定；异常走可观测、可恢复的诊断流程。

这是中等偏大的跨层修改，不是重新开发工作流引擎。预计涉及十余个现有文件，
另需迁移和测试；具体代码行数应在阶段 1 完成后重新估算，不作为交付指标。
复用 Canonical State、append-only events、operation ledger、lease、人工等待及现有 watchdog。
不引入新工作流框架，不重做前端，不承诺任何模型一定能完成指定学术任务。

## 当前已核实的缺口

- `capabilities.py` 默认给节点配置总执行时长，学术配置还设置循环、调用与重试上限。
- `worker.py` 把完整 Agent/tool loop 放入总时长 timeout；重试判断复用 NodeRun.attempt。
- `academic.py` 缺少配置时仍默认三轮；重复研究输出直接进入 blocked，缺少完整循环诊断。
- `supervisor_service.py` 会调用运行预算过期处理，却尚未接入 AdaptiveWatchdog 的进展评估。
- `watchdog.py` 已有保守的观察规则，但缺少完整证据采集、持久化诊断和恢复闭环。
- 示例、启动脚本、本机 runtime 配置及已发布 Graph Version 均可能保留旧默认值。

## 不得破坏的边界

1. 不以更大的默认 max 值或改名后的计数器替代真正修复。
2. 基础设施仍可有请求超时、连接池容量、并发限制、来源限流、文件大小及模型上下文窗口限制。
   它们处理资源和故障，不能直接等价于“研究失败”。不能删除所有名为 max 的字段。
3. 用户主动、明确启用的费用/时长等应急策略可保留，必须可见、可追溯，默认关闭。
4. 心跳证明存活，不证明研究进展；新 UUID、状态修订、文本改写和新 artifact 不自动算有效进展。
5. 无新进展证据只是未知，不足以自动失败。重复完整循环触发诊断，不自动终止。
6. 未知外部副作用必须 reconciliation；不得盲目重试或偷取仍可能存活的 worker lease。
7. 引用真实性、来源匹配、完成检查和用户证据要求不能为了跑通而放宽。
8. 保护现有未提交修改；禁止泄漏密钥。不得擅自发布新版本、重启生产服务或启动付费调研。

## 阶段 1：策略与状态契约

先提交限制清单和状态转换说明，暂不改运行行为。

- 扫描 runtime、state、API、脚本、示例、SDK 调用：列明限制位置、默认值、作用范围和失败路径。
- 将限制分为任务行为限制、传输保护、资源容量、显式操作员策略，逐项决定保留或调整。
- 定义业务循环、一次节点执行、一次请求重试的独立身份；不要改变历史 attempt 的含义。
- 定义 observing、等待上游恢复、疑似停滞、待诊断、等待人工、不可恢复错误等状态如何映射
  到现有 Run/Node/lease 状态。先检查现有契约，避免并列创建第二套业务状态真相。
- 明确诊断的证据、权限、可采取的动作及失败处理；诊断模型也不例外享有无限权限。

验收：评审状态表及迁移设计，确认不是“去掉上限，然后无限重试”。

## 阶段 2：解除默认行为限制，拆分重试语义

主要范围：`capabilities.py`、`worker.py`、`agent_tools.py`、`academic.py`、`model_gateway.py`、
`schema.py`、`relational.py`、`protocols.py` 及相关迁移。

- 普通及学术工作流默认不按轮数、调用总数或 Agent 总时长停止；检查缺省值和 SDK 隐式限制。
- 保留物理上下文/单次输出容量边界；触及这些边界时保留已验证事实和待办，不伪装成完成。
- 失败恢复记录与业务循环计数分离；一次新业务循环不能消耗故障恢复次数。
- 408/429/5xx、断流与连接故障先分类，尊重 Retry-After，按退避及供应商健康状态恢复。
  持续故障进入可恢复等待/诊断，不无限热循环，也不让默认计数器判定研究失败。
- 持久化错误分类、恢复动作、下次可尝试时间、原节点/请求关联；重启后不丢失恢复计划。
- 401/403、错误模型/协议等不盲目重试，明确暴露配置问题及恢复入口。
- JSON 修复必须保留事实；重复格式错误交给诊断，不能通过重复生成伪造证据。
- 仅对可证明安全的动作自动恢复；保留 operation ledger 去重、未知结果和人工恢复边界。

验收：循环和重试互不干扰；故障与 worker 重启后仍保留证据及可恢复状态。

## 阶段 3：接入进展观察与诊断

主要范围：`watchdog.py`、`supervisor_service.py`、证据检查、状态存储及其测试。

- 从持久化上下文、工具结果、证据验证、已完成循环和人工等待采集 ProgressEvidence。
- 指纹包含完整循环的相关输入、结果和状态；排除时间戳、随机 ID 等无关噪声。
- 有效证据增长或已验证问题解决支持继续；仅有活动、慢生成或暂时无证据时继续观察。
- 相同完整循环产生等价结果时登记去重的诊断请求，检查输入映射、反馈遗漏、来源访问、
  证据要求与工具能力不匹配等原因，不只比较 manuscript 文本。
- 持久化 observation、证据引用、诊断结果及后续动作；监督进程重启不重复发出同一诊断。
- 可修正且在权限内的问题生成明确修订工作；需要权限、配置或用户决定时进入人工等待。
- watchdog 自身故障只显示监督不可用，不误杀业务。仅凭规则实现不能宣称已完成自适应检测器。

验收：能证明“为何继续、为何等待、为何请求人工”，不能只输出健康标签。

## 阶段 4：API 与前端说明

主要范围：现有执行 API、`execution.ts`、`GraphExecution.tsx` 及执行详情。

- 展示业务检查轮数、节点执行记录和故障重试记录，三者不混用。
- 区分正在研究、等待上游恢复、预检需修订、诊断中、等待人工和最终失败。
- 展示最近已验证进展、故障原因、下次恢复动作和诊断证据；不要将观察不足显示成失败。
- 错误通过结构化、脱敏字段展示；不能把供应商可能含密钥的原始响应直接送入浏览器。
- 操作员停止优先于所有排队的恢复动作；在可证明安全的状态下提供既有或新建的恢复操作。
- 保持已修复的图连线、测量状态、服务端计时及历史结论显示。

验收：浏览器测试覆盖长运行、重试、诊断、人工等待、恢复、取消及重启后的状态一致性。

## 阶段 5：迁移与部署交接

- 更新 README、学术示例、启动/升级脚本，移除不经用户选择便写入的任务上限。
- 将旧限制列成迁移报告，区分框架默认与显式用户策略；无法区分时请求操作员决定。
- 不修改已发布不可变 Graph Version，不篡改旧 Run/事件，也不静默取消用户策略。
- 给出新版本发布、配置生效、worker 安全重启和回退步骤；失败旧 Run 的重跑或续接必须明确区分。
- 当前开发库和密钥文件仅做必要脱敏检查；实际迁移/发布/重启及在线调用另经用户授权。

## 必须通过的验收矩阵

使用假时钟、可控模型与故障注入，不靠让测试真的跑半小时证明长运行。

| 场景 | 必须观察到的结果 |
| --- | --- |
| 有已验证进展，超过旧 3 轮/30 分钟/工具调用数 | 默认继续，不产生额度耗尽失败 |
| 慢生成且 worker 健康 | 不因旧 Agent 总时长上限杀死任务 |
| 仅心跳或 UUID、文本变化 | 不误判为已验证进展，也不因此自动失败 |
| 完整循环重复且无已验证进展 | 一条可追溯诊断，非自动失败 |
| 连续 503/429 后上游恢复 | 持久化等待与退避，恢复执行；不重复已完成副作用 |
| 在多次业务循环后发生传输错误 | 与首轮相同的恢复语义，不受历史循环次数影响 |
| 401/错误模型/不兼容协议 | 清晰配置错误，不持续盲试 |
| 部分流式响应后断开 | 保留诊断材料，不发布不完整结果为成功 |
| watchdog/worker 重启 | 恢复观察与重试计划，不遗失待办、审批和证据 |
| 来源不可访问或证据不足 | 修订或明确人工处理；不得绕过引用检查 |
| 工具结果 outcome_unknown | 进入 reconciliation，不自动重跑 |
| 用户停止与重试到期并发 | 不再接纳新的工作；已发出操作结果如实记录 |
| 显式用户预算 | 可见、可审计地生效；未启用时不出现隐式上限 |
| 切换 Run、轮询、移动端 | 连线保留，历史/当前状态及计数不串扰 |

## Agent 交付要求与验收方式

阶段 1 契约先评审；阶段 2–3 完成后再接阶段 4。最后迁移交接，不将所有改动堆成一次提交。
执行 agent 需先完整阅读 README 开发原则、DECISIONS 相关 ADR、ARCHITECTURE 与 HANDOVER 的约束，
检查工作树及当前分支；本计划不授权提交、推送或覆盖其他人的改动。

每阶段提交变更清单、测试命令及结果、状态/事件样例、未解决风险和回退说明。
状态库新增合同须覆盖 SQLite 与 PostgreSQL；环境不具备时明确标注未验证，不报全绿。

验收者独立审查 diff，运行回归与故障注入，核对实际事件和前端截图，不只采信 agent 的总结。
最终经授权用 Pi 的 DeepSeek 模型 `deepseek-v4.1-flash-expires-on-0910`
（provider `deepseek`，base_url `https://api.deepseek.com/v1`，Responses API；调用方式见
`.pi/agent/models.json`）做一次真实联网调研：成功必须有通过验证的 Markdown 产物；
因真实证据或访问问题进入可解释的人工等待只能算恢复机制验证成功，不能算论文交付成功。
单次模型返回 OK、删除几个 max 参数、或只通过单元测试，均不构成最终验收。
