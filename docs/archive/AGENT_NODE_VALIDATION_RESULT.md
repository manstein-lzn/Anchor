# G2 主验收：第四次交付

> **历史归档（2026-09-25）**：保留当时的讨论、计划与验证证据，不代表当前实现或待办。
> 文中的“下一步”“待实施”“必须”及旧阅读顺序仅适用于当时阶段，不作为后续开发指令。
> 当前入口见 [项目 README](../../README.md)，唯一升级方向见 [Plugin 设计](../plugins.md)。

结论：**G2 结构性验收通过，可进入迁移 ADR/范围评估；不等同于生产级恢复或 exactly-once。**

独立复验：

- 全量 `tests/`：233 passed，无 skip
- `scripts/recovery_windows.py --only B5,B8`：B5 `shared-and-stops`、B8 `graph-closed`
- `ruff check src/ tests/ scripts/`：通过
- `mypy src/anchor/`：通过

已确认的关键结果：

- R1：失败/超时/非法提交不会被恢复误判为完成；有效提交使用结构化完成事实。
- R2/B5：主模型和摘要请求共用一个持久化计数器；本次证据为 7+3=10、10+6=16，达到 16/16 后零新请求。
- R3/B4：双流逐流预览，恢复进程可读完整或明确标记为片段的输出，挂载只读。
- B1–B4、B6–B8：压缩恢复、压缩边界、副作用间隙、追加记录、完成控制流和图恢复均有执行证据。
- B8：图恢复按自身 pass 记录跳过已完成节点，`write`/`count` 未重复执行；后续 `finish` 完成，节点内部提交事实与图记录一致。

保留边界：

- 不提供 exactly-once、断电耐久性或生产级 crash recovery。
- 真实 provider 窗口拒绝、摘要质量、引用真实性、多文件事务性未验证。
- `sandbox.py`、`execenv.py`、`context.py` 仍需主集成确认所有权、清理责任和生产配置。
- 报告首页写的基线 SHA 应更新为本次实际 HEAD `90df4e2`；不影响运行证据，但需修正文档一致性。

因此，三包组合已满足进入 ADR 和正式迁移范围评估的条件。切换默认执行器仍应另立迁移变更并单独验收。
