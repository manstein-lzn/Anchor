# 认知状态架构白皮书

**状态**：思考记录，非批准设计。本文是一次头脑风暴的整理，用于保存推理过程与证据。
正式决策仍以 `DECISIONS.md` 的 ADR 为准。

**日期**：2026-09-11
**范围**：Agent Graph 在长任务下的认知与状态框架

---

# 第一部分 · 问题

## 1.1 我们要解决什么

一个 agent graph 在长时间工作中会同时坏掉四件事：

| 失效 | 表现 |
|---|---|
| **上下文爆炸** | 每轮 prompt 越来越大 |
| **注意力被稀释** | 关键信息淹没在材料里 |
| **被现象带偏** | agent 被最近的、最显眼的信号俘获，丢掉目标 |
| **认知漂移** | 反复压缩之后，agent 对任务的认知逐步偏离实际 |

目标不是"记住更多"，而是：

> **让 agent 随时间变得老道，而不是进步的失忆者。**

## 1.2 实测证据

> **本节于 2026-09-11 经外部评审重写，初版有一条主结论算错、多处证据来源标错。**
> 错误与修正见 §5。**本节数字均可用 `research_cognition_review/verify_local.py` 复算。**

run `f4eb8ac7-3418-4e8d-8be5-8eaede46e02a`，37 条 `node_runs`。

### 已核实且可复算

```
write attempt 0 组装 prompt      141,033 字符
write attempt 1 组装 prompt      403,340 字符
一份 evidence                    137,094 字符
```

**证据一：修订轮 prompt 中，三个与顶层完全重复的字段占 34.92%**

```
feedback.evidence  == evidence     （规范化序列化后同一字符串出现两次）
feedback.plan      == plan
feedback.request   == request

删除这三个字段：403,340 → 262,492 字符，减少 140,848 = 34.92%
```

**这是重复的全部可确证部分。** 初版写的 60% 是错的：它等于
`137,094 + 106,672`，而 106,672 是**整个** `verified_sources` 列表的长度，
不是与 `evidence.sources` 相同的字节数。

**证据二：`sources` 与 `verified_sources` 是同源的两种表示，不是重复**

```
两项都是 59 条，id 集合相同，四个共有字段值相同
但 verified_sources 独有：abstract / authors / title / year / url /
                          evidence_level / read_truncated
```

**同源 ≠ 同信息。** 删它可把 prompt 降到 155,800 字符（-61.37%），但那是一个
**下游需求裁剪候选**，不是已证明的无损去重——是否可删其独有字段，需要检查
writer 的实际职责与质量影响。

**证据三：重复的可追踪机制**

```
check 返回整个 snapshot + review + verified_sources
  → check→write 边把整个 outputs.check 映射成 feedback
  → 同一条边又映射其 evidence / plan / request
  → JSON 序列化自然重复这些子对象
```

**`build_input_snapshot()` 已经支持点路径字段选择。** 详见 §3.3。

### 本次的真实对照实验（由评审执行）

只从 `feedback` 删掉与顶层完全相同的三个字段，其余全部保留：

| 指标 | 原输入 | 三字段去重 |
|---|---:|---:|
| prompt 字符 | 403,340 | 262,492 |
| 输入 token | 173,519 | 105,238 |
| 输出字符 | 14,379 | 14,667 |
| 耗时 | 48.47 秒 | 34.71 秒 |
| 结构 / 行文规则错误 | 0 | 0 |
| 漏引 / 未知引用编号 | 0 / 0 | 0 / 0 |

**可以得出的**：简单输入去重已有显著的确定性体积收益；本次去重输出没有触发这些有限检查。

**不可以得出的**：质量更好、引用语义正确、费用更低、延迟稳定改善、认知漂移被修复。
两组各一次、顺序固定、缓存状态不同、费用字段为 null，也未跑完整语义评审。

### 来源需更正、或无法核实的条目

| 初版写的 | 实际情况 |
|---|---|
| 缓存命中 99.9% | 那是**事后热缓存**重跑的测试台数字。原 run 修订轮记录的是 `67,456 / 173,519` ≈ **38.9%**，首稿 `640 / 69,135`。**不可把事后热缓存当作原运行的性质** |
| 同输入输出 14,544 / 14,573 | 原始成对实验文件未定位，**无法独立验证**。原理上应区分"输出不同"与"质量方差大"，字符数差不能量化后者 |
| 40 秒 / 46 倍 | 原 run 跨度约 1,844.55 秒，单次原输入调用 48.47 秒。节点级实验确实可行，但"整轮任务 vs 一个无工具节点"**不是同等工作量** |
| 57% 段落 / 116 标签 / 35.5% | 来自 ADR-040，但无足够定位可复算。**标为未验证，不用它支持架构收益** |
| 三轮 0 major、6/6/4 minor 仍 revise | 已核实工件，但那来自**另一个 run** `b8fdfc5d` 的 attempt 2/6/8。目标 run 是一轮确定性 major + 一轮 7 minor。**初版把整节统一归于同一个 run，错了** |
| instructions 占 1.0% | 是体积比，且 instructions 通过独立参数传给模型、不在上述字符数内。**这不是注意力分配的测量** |

### 一句校正

初版写"修订轮把 prompt 推到 2.9 倍，其中六成是同一批事实换位置再说一遍"。

**准确的说法是**：修订轮把 prompt 推到 2.9 倍，其中 **34.92% 是三个字段的完全重复**；
另有约 26% 是同一批来源的另一种表示（同源、含独有元数据）。

---

## 1.3 通行答案的问题

社区（含 OpenAI / NVIDIA / Google / 社区扩展）的通行做法是：

```
上下文快满 → 调用模型生成一段历史总结 → 拼接为
系统提示 + 工具提示 + 总结 + 最近几轮
```

**它的问题（初版写了四条，评审推翻其中两条）：**

1. **时机** —— 等到满了才压缩，前面每一轮都已按完整上下文付费。**成立。**
2. **信息** —— 压缩那一刻不知道后面什么重要；"什么重要"只在**使用时**才知道。**成立。**
3. ~~不可测~~ —— **被推翻。** 存下输入/输出、摘要版本与行为实验，摘要同样可测。
   **"是散文"不决定能否审计。** `pi-smart-compact` 已给出覆盖验证与可追踪修复的先例。
4. ~~不可审计~~ —— **被推翻。** 同上。压缩的**决策**可以记录，与它是否由模型完成无关。

### 调研到的四种方案（2026-09 一手来源）

| 方案 | 做法 | 关键局限 |
|---|---|---|
| **OpenAI** `OpenAIResponsesCompactionSession` | compaction 进服务端 API (`responses.compact`)；**SDK 侧表现为 session 的清除-重写** | 不透明；官方承认会阻塞流式、恢复失败时历史可能保持未恢复。**须区分 SDK 的 session 替换操作与压缩输出的语义**——官方指南说明返回窗口可含保留条目，API 参考还列出用户消息与 compaction item，**不能推成所有内容均被模型重写** |
| **NVIDIA** `NVlabs/SoL-Pi` | 四个机制：Action Fusion / **ObservationPack**（重复大结果→稳定句柄+分页召回）/ Evidence-Preserving Reducer（保留引用须匹配归档原文）/ **Online Context Compact**（按计划步骤边界触发，用 `cacheWriteReadRatio`=12.5 做经济决策） | 效率机制；依赖 Pi 宿主。**但 ObservationPack 确实在做"大结果外置 + 精确分页召回"** |
| **社区** `pi-smart-compact` | **EESV 管线**：Extract(0 次 LLM) → Explore → Synthesize → Verify；**验证失败则拒绝**；确定性质量地板；Continuity Ledger；FTS5 上下文图 | **原文有工作状态、跨会话项目隔离召回与覆盖验证，不是"只在会话内"、也不是"单纯散文摘要"** |
| **Google ADK** | `EventsCompactionConfig`：token 阈值 + 滑动窗口；`session.state` 与 `events` 分离 | 还列出 `include_contents` / `output_key`，**不应只读压缩页就断言整个框架没有输入选择机制** |

### 初版在这里犯的错

初版写：

> 四者的共同点：都在解决"对话历史累积"。**没有一者在解决"这一步需要什么"。**

**这是错的，而且证据就在我自己的表里。** 评审指出：

```
LangChain 官方上下文工程   明确把 select 与 write/compress/isolate 分开
Anthropic 官方实践         讨论轻量引用与按需加载；code execution with MCP 做执行侧过滤
MemGPT                    工作上下文/消息队列/外存三分，函数访问 + 分页检索
SoL-Pi ObservationPack    外置大结果 + 精确分页召回
pi-smart-compact          跨会话项目隔离的 FTS5 召回与覆盖验证
```

**准确的表述是**：这些方案分布在一个谱系上——**有的只压历史，有的已经在做输入选择与外置召回**。
本项目的差异**不是"别人都没做"，而是"在这个图里怎么集成"**。

### 一个被评审纠正的立场错误

初版的评审简报里写："验证覆盖面/证据纪律"是与 RAG 相反的立场。

**错。** 召回决定**如何取得材料**，验证决定**哪些断言可接受**——**两者正交**。
把它们对立，会使相关工作筛选偏离真正的比较维度。

---

# 第二部分 · 思想

## 2.1 重新定义：状态是充分统计量

来自原始 Anchor（见附录 A）：

```
minimize size(Project(S))

subject to:
  一个全新 Agent，只看 Project(S) + 近期窗口，
  仍能选对下一步，而无需从完整历史重建任务
```

> **State 是任务特定的充分统计量。**

并给出一条**可证伪的预测**：

> 在任务复杂度稳定时，投影出的上下文应趋近稳定大小，
> 即使 transcript 长度与 Checkpoint 数量持续增长。

这一句回答了"如何保持上下文最优"：**不是逐时刻最优（无法定义），而是维持一个充分统计量并确定性投影。**

## 2.2 遗忘是注意力分配

> **遗忘不是存储失败，也不是物理删除。它是把信息刻意移出当前注意力。**

每个 item 只问一个反事实问题：

> **如果下一个 Agent 看不到它，会不会做错决定、违反约束、
> 重复失败或昂贵的工作、或丢掉必要的下一步？**

答案决定**认知层级**（明确说：不是存储层级）：

| 层级 | 处理 |
|---|---|
| **Active Cognition** | 影响当前决策与行动 → 始终投影 |
| **Dormant Knowledge** | 现在不可行动，但丢失会让未来恢复昂贵/不安全 → **一个简洁精确的恢复引用** |
| **Event Archive** | 不再预期影响正常工作 → 仅供显式恢复/审计/调试 |

而召回是 `explicit / targeted / bounded / recoverable`，且：

> **恢复的材料不会仅因为读过一次就永久进入投影。**

> **评审更正**：单个引用小，**不代表引用集合有界**。而且未被索引或线索过弱的材料，即使可寻址也可能永远想不到召回。


**这就是"知识积累而不膨胀上下文"的答案：休眠知识是引用，不是文本。**

## 2.3 认知是因果模型，不是编年史

按**认知义务**分节（不是按话题）：

```
Situation   当前理解 / 已确认事实 / 活跃假设 / 未解冲突 / blockers
Experience  仍然约束选择的决策 / 失败路径+原因+重试条件 /
            仍然改变未来策略的教训
Intent      当前指令 / 已接受的下一步 / 下一计划 / 开放问题
Knowledge Index  当前相关的休眠引用
```

核心公式：

```
Contract goal − Situation = current gap
current gap + Contract constraints + relevant Experience = Intent
```

而最能说明问题的是那个对比例子：

```
✗  "命令 X 跑了并失败。"                      ← 事件，天然受近因支配
✓  "路径 X 失败，因为条件 Y 不存在；           ← 条件式，对近因免疫
    在有证据 Z 之前不要重试。"
```

**前者是日志，后者是认知。**

> **评审更正**：条件句**可以编造因果**，还可能固化错误的"不要重试"。该性质必须用干预与接管实验验证；§4.4 自己承认它并非可检查的因果结构。


> **这解释了状态为什么能治"被现象带偏"：不靠更好地摘要，靠换一种表达形式。**

## 2.4 投影：主要靠排除法定义

```
固定 prefix（system / developer / tool）
+ 不可变的 Task Contract 核心
+ 最新 Active Cognition
+ 紧凑的 Knowledge Index
+ 宿主的 active window（原样不动）
```

**明确排除：**

```
持久化对象 / receipt / Transition Certificate
指令散文史 / 已解决问题的细节
完整 artifact 内容 / 大工具输出
重复的事实              ← 我们 60% 的 bug
旧 Checkpoint 或摘要链   ← 压掉"摘要的摘要"漂移
```

> **投影是确定性本地工作：不调用 LLM、不扫完整 transcript、不重放每个 Checkpoint、不设私有 token 预算。**

## 2.5 frontier —— 抗漂移的形式化

```
Checkpoint = State revision + source frontier
```

> **没有 source frontier 的 Checkpoint，不允许取代 transcript 中的任何内容。**

由此职责干净划分：**frontier 之前由 Checkpoint 承担，之后由 active window 承担。**

## 2.6 分权：代码验证什么，模型判断什么

`Transition Certificate`：每个曾 active 的 item 恰好获得一种处置。

```
carry      保持激活，身份与含义不变
revise     保持激活，但被指名的新证据修正
resolve    因问题已有结果而离开激活集
supersede  因更新的指令或事实而离开
demote     离开详细注意力，但保持精确可恢复
archive    不再具有预期未来行为价值
```

配套的确定性校验能保证：

```
没有旧 item 静默消失 / carried 仍存在 / revised 有后继
新权威事实引用允许的不可变来源 / 用户更正显式 supersede 旧指令
失败没有被转写成成功完成的证据 / demoted 有内容绑定的恢复路径
Task / Contract / parent / frontier / provenance 仍然有效
```

而这里有一句是全篇最关键的取舍：

> **证书让遗忘可问责，而不假装代码能判断所有自然语言的真假。**
> **模型仍决定语义相关性。校验器建立的是覆盖面与证据纪律，不是普遍语义正确性。**

**这解决了"什么算好无法写成谓词"的困境：不去验证质量，去验证"没有任何东西静默消失"和"证据纪律成立"。后者是可判定的。**

> **评审更正**：证书能保证的首先是**已登记 item 的处置覆盖**。Episode 中从未纳入的关键内容不在旧 item 覆盖集内——它管不到"你没想到要看的东西"。而**语义正确不能由合法 source 前缀推出**。


## 2.7 PyTorch 类比：把记忆问题换成优化问题

```
PyTorch                     Agent Graph
────────────────────────────────────────────────
计算图（固定拓扑）      →    graph（不可变版本）
输入 tensor             →    run 的输入数据
前向传播                →    一次 run
权重                    →    capability 参数：prompt / 工具矩阵 /
                            上下文策略 / 边契约
loss                   →    输出质量
反向传播 + optimizer    →    观察者 + 归因（见第三部分）
```

它给出的最重要一句话：

> **老道 = 参数变好了。失忆 = 参数没变。**
> **记忆不是老道的载体，记忆是 optimizer state —— "哪次改动带来了哪个结果"的证据。**

> **评审更正**：**这把训练权重、系统提示、可复用技能、非参数经验混成了"参数"。**若把记忆也定义为参数，则成同义反复。反馈驱动的技能/经验更新已有行为迁移先例（Voyager / ExpeL / Reflexion / GEPA）。


**这解释了为什么社区的记忆系统给不出 RSI：它们存的是激活，不是优化器状态。**

### loss 有三层

```
L_deterministic   快、免费、精确      ← 结构/引用可验证/行文规则（已有）
L_judged          中速、有噪声        ← 评审（已有）
L_outcome         慢、稀疏、真实      ← 人用不用、代码在生产过不过
```

**而且有一个神经网络没有的优势：`L_deterministic` 是 per-node 的。**

> **评审更正**：**这条是错的。** Deeply-Supervised Nets（Lee et al., AISTATS 2015）在中间隐藏层设辅助目标，**深度监督就是中间 loss 的直接反例**。而且"有没有有效局部目标"与"能否识别唯一因果责任"是两件事。


```
结构/行文规则 → 打到 write
引用可验证性  → 打到 gather
一致性        → 打到 review
```

**所以信用分配不必只在 episode 末尾做。** 图里节点是离散的、命名的、数据流有迹的——这比在 10⁹ 个匿名权重上做信用分配容易得多。

### 类比在哪里断裂（五条）

1. **没有梯度** —— 只能说"离散组合搜索"。但**"梯度"可以由模型提供**：模型能说"这次失败是因为证据不足还是指令不清"，那是一个启发式提议分布。比 autograd 弱，但远不是零。
2. **variance 致命** —— 实测同输入两次输出不同（14,544 vs 14,573）。**如果评估本身有噪声，你会把噪声当信号。**
3. **参数不独立** —— 改 writer 的 prompt，reviewer 看到的东西就变了。这是个耦合系统。
4. **拓扑可能本身是错的，而调权重修不好** —— 我们亲身撞过：最初的学术图让 `research` 一个节点包办搜索+读+分析+写。那需要**拓扑改动**，不是参数调整。
   → 由此得出的纪律：**拓扑改动 = 架构搜索，稀少但有时必须；平时应抵抗拓扑 churn，优先调参数。这是我们之前缺的纪律。**
5. **最根本：PyTorch 的 loss 是给定的，这里"定义 loss"本身就是全部研究工作。**

### 关于噪声的必要澄清

"噪声"只对**边际改进**成立：

```
边际改进：质量分 8.0 → 8.2        → 需要统计，可能不值得
明显缺陷：要论文却交出审计台账      → 一次就能看见，不需要重复
```

所以本文后续讨论的是**明显缺陷**，不是 5% 的微优化。

（另需更正一处早期表述：我说过"确定性 loss 的方差是零"，**不准确**。检查本身是确定函数，但通过与否取决于模型这次写的东西，仍会变化。）

---

# 第三部分 · 综合到 Agent Graph

## 3.1 为什么它更适合图（用户的判断，我认为成立）

原始 Anchor 有一大块机制在**人为制造确定性**：

```
Plan before authority（"首次 prompt 是意图的证据，不是完整任务状态"）
Grill 式 Planning → sealed proposal → 用户确认
首次 compact 时 bootstrap 一个 provisional Task
read-only 直到提案被接受
```

**这整套东西存在的原因是：聊天场景下"任务是什么"不可先验得知。**

而图场景下，这个谈判**已经在写图时完成了**：

| 原始 Anchor 要谈判出来的 | 图里天然有 |
|---|---|
| goal / acceptance criteria / constraints | 运行输入 + 图契约 |
| execution plan | **就是拓扑本身** |
| verification | 节点/边校验约定 |
| risks | 边上的失败路由 |

> **图省掉的不是"状态"，而是"状态的不确定性来源"。**

> **评审更正**：**形式上发布图，不证明验收标准与用户目标一致。**本会话最初的错误交付契约（产出了审计台账）恰恰是反例。运行输入也可能缺项或冲突。


## 3.2 但确定性住在哪一层（关键补充）

```
图拓扑        → 确定（会发生哪几类事）
边契约        → 确定（必须交接什么）
───────────── 以上是图给你的，聊天给不了 ─────────────
节点内部      → 不确定（工具循环里模型自己决定）
循环迭代次数  → 不确定
```

**漂移问题恰恰住在下半层。**

> 直线 DAG 几乎不需要状态——它天然无漂移。

> **评审更正**：**无循环只避免"循环累积"。** 上游有损转述、错误假设、节点内压缩仍会让后续节点偏离目标。

> **真正会漂移的是循环**：`coverage → gather → coverage → ... → write → review → write`
>
> **循环是图版的"对话开得太久"。**

所以 `Checkpoint` / `frontier` 这套东西，**该放在循环边界上**。

## 3.3 工作集问题（本节已被重写——初版的归因是错的）

> **初版把本节称为"全文最重要的论断"，内容是：**
> *"原始 Anchor 把工作集委派给了宿主的 active window，而图没有那个宿主。
> 这才是 60% bug 的成因，而它是空的。"*
>
> **评审推翻了它。** 保留原文在此，是为了记录错误。

### 真实的可追踪链条

```
check 返回整个 snapshot + review + verified_sources
  → check→write 边把整个 outputs.check 映射成 feedback
  → 同一条边又映射其 evidence / plan / request
  → JSON 序列化自然重复这些子对象
```

**而 `build_input_snapshot()` 已经支持点路径字段选择。**

> **"下游声明要什么"的基础机制不是空白。当前映射只是声明得太宽。**

### 反事实检验（这是推翻的关键）

**保持没有 Pi 宿主、没有 Cognition、没有 Checkpoint 的现状，仅收窄输入，
就能去掉本例已证实的全部重复（34.92%）。**

所以"缺少宿主窗口"**不是这个缺陷不可缺少的解释**。而且——**添加一个窗口
也不自动防止边把同一对象传两遍。**

### 于是"工作集该住哪"其实是四个不同的问题

| 问题 | 对当前项目的最小回答 |
|---|---|
| 大材料保存在哪里 | 复用已有不可变工件与内容引用；**无需先建新的记忆存储** |
| 节点可以看什么 | I8 与已选入边的声明输入；**不能由全局认知库绕过** |
| 一次调用实际加载什么 | 节点输入适配/投影；**先用静态字段选择、领域转换**，再评估按需读取 |
| 单次工具循环如何避免超窗口 | 属于节点运行时的上下文管理；**不是"给整个 run 加一个 State"能完成** |

### 一个初版完全没想到的关键点

**对当前无工具的 writer，单给 59 个休眠引用没有用——它没有能力解引用。**

所以"休眠知识 = 一个引用"在实践中要求二选一：

```
预先物化所需片段             ← 就在声明阶段解决
或明确增加受账本管理的读取能力  ← 这是执行能力扩张，不是免费实现细节
```

**而且一个去重后的 59 项账本仍可能太大。** 那时才需要测分段处理、任务分解、
选择性读取及汇总损失。

> **不能把"消除复制"和"压缩任务本身所需信息"合成一个验收指标。**

### 本节的结论

```
缺陷是真的        —— 34.92% 的重复可确证
成因是映射过宽    —— 不是"缺少状态层"
因此优先级要改    —— 先修映射；状态层的必要性尚未证明
```

---

## 3.4 三个时间视野，与两类跨 run 状态

| | 当前 run | 历史 run | 未来 run |
|---|---|---|---|
| 生命周期 | 这个 run | 永久（可寻址） | —— |
| **真值来自** | **本次运行的证据** | **跨运行的证据** | —— |
| 失效方式 | run 内漂移 | **陈旧 / 过度泛化** | 投影错东西 |
| 规模 | 有界（充分统计量） | 无界（归档） | 小（必须小） |

**关键差别在"真值标准"那一行。**

原始 Anchor 的真值标准是本次运行的证据（`sources: ["episode:4:tool-result:2"]`）。而跨 run 面对一个它从未面对的问题：

> **一条在 run A 中学到的经验，凭什么在 run B 中成立？** run B 的课题、代码库、约束都可能不同。

**而跨 run 状态其实是两种相反的东西：**

| | **内容**（substantive） | **经验**（procedural） |
|---|---|---|
| 例子 | 59 篇来源、24 组对立、一篇论文 | "arXiv 限速 3s"、"评审只报 minor 就该批准"、"按产出物切节点而非按阶段" |
| 真值来自 | **provenance** | **outcome** |
| 规模 | 大 | **小** |
| 跨课题泛化 | ❌ 不该泛化 | ✅ 应该泛化 |

> **评审更正**：**这由字段类别决定不是事实，是经验问题。**内容可以跨任务复用；程序性规则也可能高度环境特定。增长率与适用性都要实验测定。

| 增长率 | 线性 | 趋于饱和 |
| 失忆代价 | 重复劳动、重复付费 | **永远不进步** |

**"老道"只来自右边那一列。**

## 3.5 统一接口：注意力层级

上面两个维度（时间视野 × 注意层级）**共用同一个判定**：

```
                  小 & 总是相关          大 & 按需
当前 run      →   本次认知              本次材料
跨 run        →   积累的经验            积累的内容
                  （自动投影）           （精确寻址）
```

> **每个 state 项声明它的注意力层级；层级决定它进入上下文的方式。**
> **时间视野不是另一套机制，只是层级判定的另一个输入。**

这条路线的好处：

- **经验小 → 能自动投影 → 默认行为被改变 → 这才是"老道"**
- **内容大 → 必须寻址 → 不爆炸**

### "老道"的真正要求

> **老道 = 默认行为被改变了，而不是记忆可以被检索到。**

> **评审更正**：**自动选择 ≠ 无条件投影。** 检索到**已验证**的程序/规则，同样能改变默认行为。本节把两种机制混为一谈了。


如果跨 run 状态只是**能被查到**，那这个 agent 不是老道，只是**多了一个图书馆**。

要真的老道，经验必须：

```
① 自动进入投影（不需要 agent 主动去查）→ 所以必须小
② 可被 outcome 验证（否则就是累积偏见）→ 所以必须带归因
③ 可被推翻（否则错误经验永久污染）    → 所以必须有退役机制
```

## 3.6 归因：缺失的那一环

原始 Anchor 在跨 run 上是**空的**：

1. **没有任何跨 session 机制** —— one session one Task；README 明确 "a fork has a new Pi session identity... never inherits writable State"
2. **明确拒绝向量检索** —— "Anchor does not require vector search. Exact references and bounded textual lookup are sufficient until real recall failures prove otherwise."

**这个立场在 run 内是对的**（你知道要什么，所以能精确寻址）。
**但在跨 run 上是错的**——新 run 开始前，**你不知道自己需要哪条旧经验**。

> **评审更正**：不知道需要什么，只证明需要**发现机制**，**不证明必须向量化**。结构化路由与 FTS 都是可比较的基线。旧项目原文是"无需，直到召回失败证明必要"，**不是永远禁止**。


而 RSI 真正缺的是：

```
State → Decision → Action → Observation → Milestone → Outcome → **Attribution**
                                                                 ↑ 这一环是空的
```

> **没有归因，跨 run 状态就只是"累积起来的观点"，不是"被验证过的经验"。**

## 3.7 run 内 vs run 后：重做 vs 改版

一个必须澄清的区分（本文作者早期曾把两者混为一谈）：

```
改「图」  → 塌方   （一个 run 必须只有一个计划版本；否则"这个 run 跑的是哪版"无解）
改「状态」→ 本该如此（这就是 Update 存在的原因）
```

**run 内到底什么能改：**

| run 内能做的 | 现有机制 | 需要改图吗 |
|---|---|---|
| 更新认知（state） | Update / checkpoint | ❌ |
| 路由回上游（证据不够 → 回 gather） | 循环 + 条件边 | ❌ |
| 重做节点（输出不合格 → 带反馈重做） | `output_retries` + `rejected_refs` | ❌ |
| 节点内换工具/策略 | 模型的工具循环 | ❌ |
| 停下找人 | `needs_input` | ❌ |
| 改节点参数（prompt/策略） | —— | ✅ = 改版 |
| 加/删节点 | —— | ✅ = 改版 |

> **run 内的杠杆是「重做」（带诊断重做）；run 后的杠杆是「改版」（改参数/拓扑，产生新版本）。**

**验证**：本次会话中所有修复（拆 gather/write、引用编号、坏引用路由、收敛判据、评审批准规则）**全部发生在 run 之间**，没有一次在 run 内改图。

**run 内的杠杆在 state 上**——这正好接上"state 才是内核"：

```
图 提供「决策点」   —— 条件、重试、循环（拓扑，固定）
state 提供「流过的内容」 —— 认知（每次都在变）
```

run 内的三种情形：

```
输出不对  → 带诊断重做这个节点
输入不够  → 路由回上游
路子不对  → 只能停下找人（因为"改路子"就是改图）
```

**第三行是关键**：run 内可以**察觉**路子不对，但**修不了**——只能记录并停下。而这条记录正是 run 后优化的输入。

> **评审更正**：**"只能停下"只在固定图未预留替代路径时成立。**若图已声明条件分支或节点内策略选择，run 内可以合法换路线。应坚持 I1（一 run 一计划版本），而非把它说成普遍的计算限制。


## 3.8 观察者：同一条 Update 协议，三个尺度

```
previous state + Episode → semantic proposal → 校验 → durable commit
```

| 尺度 | Episode | proposal 的内容 | 提交到 |
|---|---|---|---|
| **节点级** | 一个节点的工具循环 | 该节点产出 | run 认知 |
| **run 级** | 一次完整 run | 本次认知 + 归因 | 认知 + **优化器状态** |
| **多 run 级** | N 次 run 的归因记录 | **参数变更提案** | **新 capability 版本** |

**三个时间视野不是三套机制，是同一条 Update 协议作用在三个尺度上。**

> **评审更正**：**可以复用提案/提交框架，不能复用信任标准。**run 内认知提交的谓词是身份/覆盖/来源/冲突纪律；跨 run 经验晋升需要**效用与适用域证据**；配置发布需要**权限与验证**。三者不能共用同一条"合法即可信"的审批标准。


### 观察者必须做与绝不能做

| 观察者的活 | 该由谁做 |
|---|---|
| 读 trace，判断失败原因 | **LLM** |
| 提出 prompt/策略改动 | **LLM** |
| 提出"这个节点该拆" | **LLM（作为提议）** |
| 计算结构检查是否通过 | **代码** |
| **判断某改动是否真的更好** | **代码 + 测试台** |
| 决定采用哪个版本 | **人 或 明确阈值** |
| 记录归因 | **代码**（LLM 只提供内容） |

> **观察者提议；测试台裁决。**
> **观察者可以改「怎么做事」，不可以改「怎么算合格」。**
> 前者是参数（prompt、策略、边契约），后者是门槛（检查、验收）。**改门槛的提案必须走人。**

> **评审更正**：**这条按字段名切会漏。**边契约可能规定"必须提供/保留哪些证据"，**它本身就在定义局部合格**。权限必须按**语义影响**分，不能按字段名分。


### 自省需要一个对立面

> **问 agent"你干得怎么样"，它会告诉你它干得很好。**

所以自省的内容应该是**差异**，不是概述：

```
✗  "总结一下这次工作"
✓  "确定性检查发现 A 项不通过、评审报了 B 条；
     解释你的产出为什么与它们之间有差异"
```

而且**观察者最好是"没干过这活的人"**——干活的 agent 对自己的假设是盲的。

这正是原始 Anchor 评审简报里的 **Fresh-Agent Takeover**：

> 一个全新 Agent，只看 Contract、投影出的认知、索引和近期窗口，
> 能否继续而不必猜关键决定、也不必从完整历史重建任务？

> **评审更正**：**takeover 与独立审计者是两个任务**：前者测"状态是否足以继续"，后者测"失败解释是否正确"，真值标准不同。而且陌生观察者**也可能共享同一个模型偏差**。


### 结构性护栏（协议级硬约束）

```
1. 观察者不提交，只提议
2. 图在 run 期间不可变；提议产生新版本
3. 确定性层是地板，不是 LLM —— 零差距的确定性兜底优先于不可验证的模型输出
4. 改动幅度必须超过实测噪声
   （**更正**：噪声只对**边际改进**成立。明显缺陷一次就能看见，但证明"修复可靠/可泛化"仍需重复与独立任务）
5. 留出集：不在被调优的输入上验证
6. 每轮改动有界，否则不可归因
7. 拒绝而不降级：宁可拒绝提案，不静默接受
```

---

# 第四部分 · 架构

## 4.1 分层

```
第 4 层  优化（观察者 · 归因 · 参数改版）      ← 未解问题住这
第 3 层  跨 run 状态（经验 · 内容 · 索引）      ← 未解问题住这
第 2 层  认知（Contract · State · 投影 · frontier） ← 设计已存在
第 1 层  执行 + 观测（已有，实测过）
```

## 4.2 边界：状态框架只做四件事

> **认知 · 注意力 · 归因 · 投影**

**执行、工具、沙箱、工作区、多智能体编排留在下面，不往上长。**

这条边界的依据来自原始 Anchor 自己的 DON'T 清单：

```
vector memory · knowledge graph · scheduler · agent pool
multi-agent governance · reviewer authority · workspace sandbox
automatic per-turn projection LLM · asynchronous Update
parallel state writers · private token budgets
```

以及它写的两句：

> **"Keep the Extension thin."**
> **"现在不要因为 RSI 很有趣而提前扩大核心 runtime。"**

**注意 `workspace sandbox` 在那份清单上，而当前 Anchor 造了其中大部分。**
这不是说那些东西没价值，而是说**它们不是核心**。

## 4.3 已有 / 缺失

### 已有

**执行底座**：图 IR / 不可变版本 / bundle · 租约/attempt/claim/恢复 · 控制·agent·验证·循环·条件·join · 声明式输入(I8) · 血缘(recovery closure) · 人工等待/审批门 · 收敛判据/机械重复守卫

**观测仪器**：模型调用录制 · 精确回放（按位置、分歧定位失败） · 节点测试台（40 秒） · 按节点用量 · 进度/诊断事件 · 日志

**判断层**：交付物确定性检查 · 评审门 + target 路由 · 批准规则（只报 minor → 接受） · 代码质量门 + 棘轮

**思想模型**：原始 Anchor 的完整文档（见附录 A）

### 缺失 —— 必须分成两类

**A 类：没造（照已有设计做即可）**

```
1. Contract 语义 —— 认知的锚；没有它算不出 "gap"
   **更正**：初版写"没有 Contract"。本地 Task 已有 objective / constraints /
   success_criteria，所以准确说法是**"尚未归一并冻结完整 Contract 语义"**。
   评审给的建议是：逻辑上显式、类型化；**物理上先复用当前 Task/run 准入与不可变内容，
   不新建 Contract 服务**。见 §4.7
2. State 作为一等对象 —— Situation/Experience/Intent/Index + 版本 + frontier + 证书
   现在只有 input_snapshot / context_snapshots，没有"认知"
3. 边作为「需求」的载体 —— 现在传的是"上游产出了什么"，
   需要的是"下游声明需要什么"
4. 投影层 —— 按公式确定性读取；现在没有，这就是 60% 重复的成因
```

**B 类：没解（研究问题，不是工程量）**

```
5. 工作集该住哪 —— 原始 Anchor 委派给宿主，图没有宿主。这是 bug 的成因，而它是空的
6. 归因 —— 一次 run 的缺陷 → 具体节点的具体参数。
   图让它可做（节点命名、数据流有迹、per-node 确定性 loss），但没人证明过它成立
7. 跨 run 经验凭什么被相信 —— RSI 的核心问题
   四项外部方案没有答案，原始 Anchor 从设计上就不跨 session
```

## 4.4 原始 Anchor 的表达力上限（诚实清单）

它能表达**任务级认知**，且相当完整。**不能**表达的有：

```
1. 没有关系 —— "No generic relation system or vector representation is part of the core"
   → "fact-17 依赖 decision-3 成立"说不出来
   → supersede 只是证书里的一次性处置，不是可查询的关系
2. 没有工作集 —— 大材料只能被引用，不能在工作集里被操作
   → 对散文任务够用；对"要转换一份 59 项证据表"不够
3. "因果模型"是散文里的说法，不是数据结构
   → 是"该怎么写陈述"的期望，不是可检查的性质
4. 置信度是二值的 —— confirmed fact vs active hypothesis
   → 长期任务里大量东西是部分已知的，二值太粗
5. 没有作用域 —— 一个 session 一份状态，全局
   → 图需要知道"这条事实属于哪个节点/子任务"
```

**它表达的是"任务层面的认知"，不是"多主体共享的工作内存"。**

> **评审更正**：**"无通用关系系统就不能表达依赖"说得太绝对**——散文可以表达依赖，只是不能机械查询/失效传播。**"二值置信度必然太粗"也不成立**：是否需要数值置信度应由具体失效证明，而且模型自报的数值**不自动更可信**。


## 4.5 四个问题：状态到底能解决多少

| 问题 | 状态能否解决 |
|---|---|
| **上下文爆炸** | **半解决** —— 治历史，不治工作集 |
| **注意力被稀释** | **半解决** —— 治量与重复；引入"压缩导致的过度自信"这一新失效模式 |
| **被现象带偏** | **能，最强的一环** —— 靠换表达形式（因果式而非编年式），而非更好的摘要。但该性质**只由 schema/提示词保证，代码不检查** |
| **认知漂移** | **消除"静默丢失"类，不消除"判断偏移"类** —— 漂移从无声变可审计；检测仍需外部实验 |

## 4.6 未解的问题（诚实摆出）

```
① 工作集该住哪        —— 知道故障形状（60% 重复），知道方向（边契约 +
                         休眠引用 + 内容寻址），但没有已验证的设计
② 归因如何成立        —— 推理上可做，但不是证据
③ 跨 run 经验凭什么被相信 —— 我完全不知道答案。
                         "老道 vs 失忆"最终落在这一个问题上
④ loss 如何定义        —— "什么算好"无法完全写成谓词，是本领域的开放问题
```

## 4.7 推进顺序（按评审建议重排）

> **初版主张"先把第 2 层做出来"。评审否决了这个优先级，理由成立：**
> **它是从一个输入映射缺陷推出的，而最小去重已经能削掉三分之一。**

### 新顺序：每步通过后才进入下一步

```
1. 先修正论断与实验记录
   区分字面复制 / 同源不同字段 / 任务必要工作集；
   把原 run、其他 run、事后 harness 的数据分开。       ← 本文 §1.2 已做

2. 只在学术图收紧反馈输入
   保留一份 evidence、原稿和必要 review；对 verified 元数据做独立需求审查。
   用已有点路径映射，**不先建通用投影语言**。
   为字段保留和大小变化加回归断言。

3. 把现有测试台变成可信实验入口
   冻结并保存实际 instructions / 模型 / 配置摘要、输入与输出版本；
   明确 memory 冻结范围；修掉"打印结果后非零退出"的可观测性问题。
   **复用已有观测能力，不重建平台。**

4. 做认知层的独立接管实验        ← 关键
   完整历史 / 普通结构化摘要 / 无证书 State / 有证书 State
   保持 Contract、工作集、模型、工具条件一致；
   注入早期约束、用户更正、错误假设、失败路径与后续反证；
   **按独立预设答案测下一行动是否正确，不能只看与原 Agent 是否一致——
     因为原 Agent 也可能错。**

5. 有独立收益后，才接一处循环边界
   先明确 I2 / I8、单写者与覆盖集；测崩溃 / 过期版本 / 缺失引用 / 漏吸收分支。
   工具节点的真实对照需受账本管理的 scratch run，**这是明确扩张**。

6. 跨 run 优化留在离线实验
   优先复用 MIPRO / GEPA 类候选提议与验证思路；
   未证明收益前，不增加在线 optimizer 服务或全局经验自动投影。
```

### Contract 的建议（评审给出）

**逻辑上显式、类型化；物理上先复用当前 Task/run 准入与不可变内容。**

```
把图定义中的默认约束、调用者明确输入、Task 要求，
在准入时归一成一个**已接受的 Contract revision**，固定引用并纳入 I2；
至少区分 goal / acceptance / constraints / non-goals。
冲突或缺项显式报告，不由观察者补成"用户已批准的要求"。
```

- **不能仅放在松散 metadata**：否则缺少校验和优先级
- **不能从拓扑推导**：同一拓扑可以服务不同目标——图结构无法推导"论文还是审计台账"
- **也不需要先决定新数据库表**才能做去重实验
- **验收标准的修改必须由人接受**；在支持契约修订事件前，默认通过新 run 使用新契约，
  **不静默改当前 run 的合格定义**

### 关于噪声的正确收窄（评审）

> **一个确定性坏例足以证明缺陷存在；**
> **一个修复后的好例不足以证明可靠修复或普遍收益。**

不必为字段相等做统计，但**语义收益和失败概率仍需重复及独立任务**。

---

# 第五部分 · 评审与修正

2026-09-11，本文经一次外部评审（`docs/COGNITION_ARCHITECTURE_REVIEW.md`，
其复算材料在 `research_cognition_review/`）。

**评审做了真实核查：** 只读连接 `api.sqlite`、按 SHA-256 校验后读取原始工件、
运行现有测试台做了两次真实模型调用、跑了两组回归测试（35 项通过）。
**本文 §1.2 的数字已由 `verify_local.py` 独立复现——包括它自己的 34.92%。**

## 5.1 评审的结论

> **不批准按初版 §4.7 直接建设完整第 2 层。**
> 保留"可问责的状态更新"作为**待验证假设**，重写证据链和推进顺序。

> **这些问题不证明认知层没有价值；它们证明现有数据尚不能为这套架构的
> 必要性、有效性和实施优先级背书。**

## 5.2 死掉的是什么

```
① "60% 字面重复"              → 34.92%（三个字段的完全重复）
② "图没有宿主工作集"是成因       → 成因是边映射声明过宽；机制本已存在
③ "第 2 层应先行"              → 它是从 ② 推出的，随 ② 一起倒
④ "没有一者解决这一步需要什么"   → 反例就在本文自己的调研表里
⑤ "摘要天然不可测、不可审计"     → 存输入输出/摘要版本/行为实验同样可测
⑥ 神经网络没有 per-node loss    → Deeply-Supervised Nets 是直接反例
```

## 5.3 幸存的是什么

**三个独立的研究假设**（§1.2 并没有测它们）：

```
① 结构化任务状态能否改善接管
② 显式处置能否减少未察觉的信息遗失
③ 条件化经验能否改善后续任务
```

**以及一件工程事实**：简单输入去重已有显著的确定性体积收益。

## 5.4 评审排出的失败方式（前四条）

| 排名 | 失败方式 |
|---|---|
| 1 | **为输入映射缺陷建设整层架构** —— 最小去重已能削掉三分之一；与"只改映射"的基线比，新增状态没有独立收益而成本和维护面上升 |
| 2 | **State/Index 变成第二份历史** —— 反事实问题偏向保守保留，而 §3.5 又要求经验自动投影；旧条目与恢复线索都会增长 |
| 3 | **带证书的错误变得更难质疑** —— 错因被写成因果句并 carry，旧认知成为后续 Update 的前提 |
| 4 | **用错误评价优化到错误目标** —— 局部格式全绿但实际质量下降 |

评审指出的"长成它要解决的问题"路线：

> 为保证不遗漏而增加更多 item → 为恢复它们而增加更多索引 →
> 为治理索引而增加归因和评审 → 再把这些产物继续注入上下文。
>
> **必须把全链路成本和行为收益作为约束，不只追求最新投影看起来简洁。**

## 5.5 最不该做的事（评审）

```
· 因为有早期 schema，就把完整第 2 层列为所有改进的前置依赖
· 同时建设认知库 / 知识图谱 / 向量库 / 归因服务 / 自修改观察者
· 让节点级观察者每次都重写全局状态，或让经验绕过 I8 自动注入所有节点
· 把录制回放、JSON/schema 合法、引用可解析、局部检查通过，
  任意一项当作新策略正确的充分证据
· 将通用"少于 major 即批准"经验覆盖领域验收，
  或让优化器通过修改边契约间接放宽质量门槛
```

## 5.6 本文照单接受的更正清单

除 §5.2 的六条外，另接受以下（已就地标注）：

```
§2.2  休眠引用集合可能无界
§2.3  条件句可以编造因果
§2.6  证书只覆盖已登记 item
§2.7  "参数"混淆了权重/提示/技能/经验
§3.1  发布图不证明验收与用户目标一致
§3.2  直线 DAG 仍会因有损转述漂移
§3.4  泛化方向不由字段类别决定
§3.5  自动选择 ≠ 无条件投影
§3.6  不知道需要什么 ≠ 必须向量化
§3.7  已声明分支时 run 内可合法换路线
§3.8  三个尺度不能共用同一信任标准
§3.8  takeover 与独立审计者是两个任务
§3.8  边契约本身也在定义局部合格
§3.8  噪声只对边际改进成立
§4.4  "无法表达依赖/置信度必然太粗"说得太绝对
简报 §4  验证与召回正交，不是对立立场
```

## 5.7 我（本文作者）从中得到的判断

**我从一个真实的工程缺陷，推到了一个不成立的架构必要性。**

缺陷是真的（34.92% 可去重），但它的成因是**映射声明过宽**，不是"缺少状态层"。

而本文初版还犯了更基础的错误：**一份声称"一切结论的地基是实测证据"的文档，
有六条证据的来源标错。** 其中"60%"和"99.9% 缓存"两条是把**事后**的数字
当成了**原运行**的性质。

**教训与本文主张的纪律是同一个**：无法验证的论断比错误论断更危险，
因为它会被当作已确认的前提继续使用。

---

# 附录 A · 原始 Anchor

本白皮书的思想模型主要来自一个**同名但不同的早期项目**，已归档在仓库的 `contextengine/`（逐字节保真，附 `PROVENANCE.md`）。

它是 `anchor-pi`：一个 Pi 扩展 + 纯标准库 Python 状态核心，主题是"为长时间运行的 Pi 会话提供持久的规划与认知更新"。

**恢复方式**：当前仓库覆盖了同名仓库，旧历史变为不可达。通过 GitHub 公开事件流（记录每次 push 的 `before`/`head` SHA）定位到 `feefcf64940e`（2026-09-04），tarball 取回。

**关键文档**：
- `docs/PRINCIPLES.md` —— 八段，最快的入口
- `docs/PRODUCT_SPEC.md` —— 四个核心对象（Task / Checkpoint / Episode / Projection）
- `docs/ARCHITECTURE.md` —— 充分统计量、注意力层级、证书、投影
- `docs/ANCHOR_UPDATE_PROTOCOL_V2.md` —— 语义操作 + 确定性物化 + 证书 + 事务边界
- `docs/ANCHOR_CONTINUITY_REVIEW_BRIEF.md` —— 风险清单与六个验证实验

**它的八条原则：**

```
1. Plan before authority —— 首次 prompt 是意图的证据，不是完整的任务状态
2. One session, one Anchor
3. State is authority —— 确认的 Task + 不可变认知 Checkpoint 存在一个 SQLite 权威里
4. **Context is a projection** —— Anchor 不在普通模型调用时重新摘要或裁剪窗口
5. **Update is a lifecycle node** —— 压缩触发一个无工具的专用认知任务；
   其输出经校验和版本化提交后才成为状态
6. **Mechanical truth stays mechanical** —— git/哈希/检查决定，不靠 agent 散文
7. **Failure is non-destructive** —— 被拒绝的更新保留先前状态与活动窗口
8. **Keep the Extension thin**
```

**它定义的定位（第四节的判断）：**

> **Anchor 是 Agent Runtime 与模型 Context 之间的 authoritative cognitive state layer.**
> 明确**不是** Agent Memory，也**不是** Universal Agent Runtime。

```
Runtime  → 发生了什么（工具、transcript、窗口、压缩边界、执行）
认知层   → 当前任务什么是真的、哪些必须保持、哪些可以降注意力
LLM      → 基于当前认知选择下一步
```

**它警告的五个风险：**

| 风险 | 判据 |
|---|---|
| State 变成第二份 transcript | 判据不是字段多少，而是"**这个 item 还会改变未来行为吗？**" |
| Belief State 成为第二套权威 | agent 自述只能用于诊断，不能替代 Checkpoint |
| 每个事件都启动 LLM capture | 成本/latency/竞争/churn → 一个边界 → 一个 Episode → 一次串行 Update |
| **任意状态预算**（`maxFacts=100`） | 大小必须由"任务复杂度 + 最小充分认知"决定，不是配额 |
| Recall 把历史污染回来 | 必须 explicit/targeted/bounded，绝不能"agent 想起什么 → dump 5000 token" |

**Risk 4 与本项目最初的原则一致**（"不要用 max_xxx 限制大模型"）——原始 Anchor 独立得出了同一结论。

---

# 附录 B · 本次会话中修正过的判断

保留这些，因为它们记录了思路的演化：

| 早期判断 | 修正 |
|---|---|
| "上下文爆炸可以由状态解决" | **半解决**。状态治历史，不治工作集。原始 Anchor 把工作集委派给宿主，而图没有宿主——这才是我们 60% bug 的成因 |
| "确定性 loss 的方差是零" | **不准确**。检查本身是确定函数，但通过与否取决于模型这次的输出，仍会变化 |
| "观察者不能改运行中的图" → 被读成"run 内无法自我纠正" | **两者说的是不同对象**。改图会塌；改状态本该如此。run 内的自我纠正是"检测 + 重做"，缺的是语义检测与诊断回灌，不是权限 |
| "回放可以用于 A/B 上下文策略" | **不能**。回放按位置匹配，改变 prompt 必须真实重新调用模型（见 ADR-044） |
| 用噪声/统计讨论优化问题 | **过度工程**。噪声只对边际改进成立；本次撞到的全是明显缺陷，一次就能看见 |

---

# 附录 C · 一句话摘要

> **实测到的缺陷是真的：修订轮 prompt 里三个字段与顶层完全重复，占 34.92%，
> 且成因是边映射声明过宽——不是缺少状态层。**
>
> **因此：修复映射是明确的工程任务；认知连续性是待验证假设；
> 跨 run 优化是靠已有先例可借鉴的离线研究。**
>
> **把三者捆成一个"先建的内核"，是本方案目前最可能的失败方式。**
>
> **要证明的命题收窄为一句：**
> **在同样的输入选择和工作集条件下，可问责 State 比结构化摘要更能保持正确行动。**
>
> 在此之前，本项目记录在案的失败模式——范围膨胀——是最大的风险。
