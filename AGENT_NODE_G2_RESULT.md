# G2 实施结果：上下文与恢复的组合验证

状态：实施完成，可审阅。**本文件不是 G2 通过的声明** ✓——主集成最终写 `AGENT_NODE_VALIDATION_RESULT.md` ✓。

基线：分支 `research/node-g2`，从 `919b70b`（含 02 的 `087ccff` 与 03 的 `919b70b`）✓。
环境：Python 3.12.3 ✓ · Linux 7.0.0-31-generic x86_64 ✓ · bubblewrap 0.9.0 ✓ ·
`pydantic-ai-slim==2.46.0` ✓ · `pydantic-ai-harness==0.32.0` ✓ · `mini-swe-agent==2.4.6` ✓ ·
`litellm==1.101.0` ✓ · `pydantic==2.13.5` ✓。**无付费 provider 调用、无凭证读取** ✓。

## 0. 结论摘要

| 底线 | 状态 |
| --- | --- |
| **安全底线**（未知副作用不重放、不误用旧快照、不重置预算、不把损坏引用当新任务） | ✅ **成立** ✓——五个 kill 窗口逐一实测 ✓ |
| **可用性底线**：已 settle 的普通工具路径 | ✅ **实际继续成功** ✓（A1 ✓）|
| **可用性底线**：压缩后恢复路径 | ✅ 成立 ✓（B1 ✓）|
| **可用性底线**：已持久化有效提交路径 | ⚠️ **节点可以，图不可以**——**图级衔接标为受阻** ✓（A6 ✓）|

**一句话**：**在节点这一层，这套 Harness 的安全与可用两条底线都立得住** ✓；**唯一立不住的是图的 finalize 那一步**
✓——提交已经有效并持久化，而图没有任何接缝能被告知这件事 ✓。

## 1. A 门槛：逐项结果

**全部为真实执行** ✓：真实 `SIGKILL` ✓、真实 `bubblewrap` ✓、真实 CLI ✓、真实 Git ✓、恢复是**新的操作系统进程** ✓。
kill 时刻由**同步屏障**决定 ✓（节点到达窗口 → 写一个字节到管道 → 阻塞 ✓；父进程读到**那一个字节**才 kill ✓，
`exit_code == -9` 与屏障文本一起进证据 ✓）。

| ID | 结果 | 关键证据 |
| --- | --- | --- |
| **A1 实际继续** | ✅ **通过** | 进程一完成副作用后被 kill ✓；进程二**只拿到引用** ✓，用**同一执行实现**继续到有效提交 ✓；`counter` 仍是 **1** ✓；1 次模型请求 ✓ |
| **A2 写入间隙** | ✅ **通过，且窗口存在** | 源码：终态写在 `after_tool_execute`（`_capability.py:622`）✓、快照写在 `after_node_run`（`:661`）✓——**两次独立写入** ✗；真 kill 落在两者之间 ✓；**旧 complete 快照存在而新副作用未被覆盖 → `uncertain`** ✓ |
| **A3 重复恢复** | ✅ **通过** | 真实入口顺序三次 ✓：`uncertain` → 0 次请求 ✓；`continuable` → 提交 ✓；`finished` → **0 次请求**且提交一致 ✓ |
| **A4 引用与损坏** | ✅ **通过** | 错 store / 错 node / 未知 run / 版本不符 / 负预算 / 类型错 / **真实损坏的 run·快照·events·预算** ✓——全部**有解释地拒绝** ✓，**零命令执行** ✓；引用里的预算不能覆盖目录里更高的已用额度 ✓ |
| **A5 真预算累计** | ✅ **通过** | 真请求 × 真中断 × 真重启 ✓；持久化预算与**模型接收端自己的计数**相符 ✓（两边独立记账 ✓，不是本测试选的数字 ✓）|
| **A6 提交与图完成间隙** | ⚠️ **受阻（明确记录）** | 真图真 agent ✓，在**节点识别提交**处 kill ✓，`counter=1` ✓、节点 trace 存在 ✓；图只记了它自己的 `start` ✓；**store 里没有任何 run** ✗——图的默认路径用 mini agent ✓，不产生步骤记录 ✓，**图也没有接缝能被告知** ✓ |

### 1.1 A 门槛顺带修正了 03 的两条结论

`AGENT_NODE_PLAN_03_RESULT.md` §6 已写明 ✓：03 说「C1 边界够不到」✗ 与「C5 窗口不存在」✗ **都是错的** ✓——
两者都是**capability 注册顺序**造成的假象 ✓。实测规律：

| 钩子方向 | 顺序 |
| --- | --- |
| `before_*` | 按注册顺序 |
| `after_*` | 按逆序 |

屏障注册在 `StepPersistence` **之前** ✓，`after_*` 钩子就落在框架写入**之后** ✓——C1 于是给出 `replayable` ✓
（计划期望的 K1 ✓），C5 给出 `uncertain` ✓。

## 2. §4 共享执行层检查

**真实执行** ✓，断言在 `tests/test_node_integration.py` ✓：

| 检查 | 结果 |
| --- | --- |
| 模型预览与共享存储额度是**两个**限制 | ✅ 40 KB 输出、10 KB 预览、1 MB 预算：预览有界 ✓、完整输出真的在盘上 ✓ |
| 双流同时输出**短**内容不丢 | ✅ 两个标记都在 ✓，且不落盘 ✓ |
| 运行期间内存与磁盘有界 | ✅ 在新解释器里量**峰值内存增量** ✓ 与**磁盘字节数** ✓（200 MB 命令 ✓）|
| 无存储配置不交出已删除路径 | ✅ 不交路径 ✓、明说「没能完整保留」✓ |
| 超时有限截止、暂存清空 | ✅ 1 秒超时在有限时间内返回 ✓，超时前的输出保留 ✓，暂存目录为空 ✓ |

## 3. §5 组合矩阵

| ID | 结果 |
| --- | --- |
| **B1 压缩后恢复** | ✅ 真实执行 ✓——上下文能力与持久化**同时开启** ✓，压缩真触发 ✓，kill 后**新进程**继续 ✓；原始记录仍能查回被压缩掉的内容 ✓ |
| **B2 压缩两侧中断** | ❌ **未执行** |
| **B3 副作用间隙（压缩后）** | ❌ **未执行** |
| **B4 输出引用（恢复后读回）** | ⚠️ **部分**：§4 的检查覆盖了读回与 incomplete ✓；**恢复进程中的读回**未单独跑 ✗ |
| **B5 预算组合（主/摘要/重试）** | ⚠️ **部分**：A5 覆盖了主模型预算跨恢复 ✓；**摘要与窗口溢出重试**跨恢复未跑 ✗ |
| **B6 原始记录关联** | ⚠️ **部分**：02 的 B6 覆盖了关联 ✓；**跨多次压缩与恢复**未跑 ✗ |
| **B7 完成控制流** | ✅ 真实执行 ✓——开着上下文与持久化时，单响应三连（提交在中间）仍然只跑到提交 ✓、1 次请求 ✓ |
| **B8 图闭环** | ⚠️ **部分**：op 不依赖任何 Harness 内部对象 ✓ 实测 ✓；**压缩后中断再恢复的闭环**未跑 ✗ |

**未执行的原因如实说明**：B2/B3 需要**在压缩前后各设一个屏障**并在两种状态下 kill ✓——机制已有 ✓（屏障 + `--recover` ✓），
但把它们组合成"压缩前 / 压缩完成但检查点未提交 / 检查点已持久化"三态 ✓ 还需一轮实现 ✓；这一轮的时间用在了 A 门槛上 ✓——
而 A 门槛是计划**明确要求先过**的 ✓。**B4/B5/B6/B8 的缺口同样是为了先满足 A** ✓。

## 4. API 改动与共享文件

| 文件 | 归属 | 改动 |
| --- | --- | --- |
| `src/anchor/node/__init__.py` | 主集成 | 新增 `UNCERTAIN` 状态 ✓、`NodeRequest.recovery` ✓、`NodeOutcome.recovery` ✓（§25 允许的最小扩展 ✓）|
| `src/anchor/node/pydantic_adapter.py` | 主集成 | 新增 `_what_a_previous_attempt_left` ✓（五个判定的唯一分派处 ✓）、`run_node(..., recovery_store=)` ✓、`_next_run_id` ✓、`_reference` ✓ |
| `src/anchor/node/recovery.py` | 03 | 新增 `finished` 判定 ✓、绑定检查 ✓、快照覆盖检查 ✓、预算校验与取高者 ✓、损坏转拒绝 ✓ |
| `src/anchor/runtime/sandbox.py` · `execenv.py` | 主集成 | 02 的输出采集改动**待主集成复核** ✓（本轮未再改 ✓）|
| `scripts/recovery_windows.py` | 本包 | 扩展：C1–C9 + A1 + A3 + A4 + A5 + A6 ✓，单命令入口 ✓ |
| `tests/test_node_recovery.py` · `tests/test_node_integration.py` | 本包 | 21 + 8 项 ✓ |

**本包 commit**：`1b2d24d`（A2 ✓）· `6f67958`（A1 ✓）· `cf19939`（A3 ✓）· `0878f40`（A4 ✓）·
`c81e994`（A5 ✓）· `46b8b9b`（抽取 ✓）· `b365457`（A6 ✓）· `cd49f80` · `0bddb1c`（§4/B ✓）·
以及 03 报告的修正 §6 ✓。

## 5. 交 G2 的四项说明（§110）

1. **快照覆盖的历史/effect 范围**：快照在**每个工具调用全部返回**的 `CallToolsNode` 之后写 ✓，
   含该轮的 pending tool-return ✓；**终态 effect 记录在它之前** ✓——所以**存在一个窗口** ✓，
   这就是 A2 ✓。判断「快照是否覆盖某个 effect」靠**逐个 `tool_call_id` 在快照历史里找** ✓，
   不是看快照存在 ✓。
2. **恢复判断**：四+一个判定 ✓——`replayable` ✓（没进过工具）· `uncertain` ✓（`started` 无终态 / 失败 /
   快照不覆盖已 settle 的 effect ✓）· `continuable` ✓（全覆盖 + complete 快照 ✓）· `finished` ✓（历史里
   已有被接受的提交 ✓）· `invalid` ✓（引用不成立或记录损坏 ✓）。
3. **终态提交**：节点层可以 ✓（`finished` 不重问模型、不重跑提交 ✓）；**图层受阻** ✓——见 A6 ✓。
4. **预算与输出生命周期**：预算是 Anchor 的 ✓（框架明确不恢复 retry counter ✓），**每次请求即记账** ✓
   （不是结束时 ✓——被 kill 的尝试也要付费 ✓），跨恢复取**已用更大、允许更小** ✓；输出引用在**被挂载的
   那一个目录**里 ✓、只有被裁的流才落盘 ✓、达到上限明确标记 incomplete ✓、本包不实现自动 GC ✓。

## 6. 发现的框架约束与未验证事项

**框架约束**（不是缺陷，是形状）：

- `run_id` 单次有效 ✓——重用一个 `run_id` 直接拒绝 ✓，所以恢复是**新的 `Agent.run`** ✓、逻辑序列走
  `conversation_id` ✓。
- capability 的钩子方向不同 ✓（`before_*` 顺序、`after_*` 逆序 ✓）——**决定一个钩子落在写之前还是之后** ✓。
- 成功路径上**不会有** `run_completed` ✓——本节点在提交后的边界离开 `agent.iter` ✓，框架把 run 记为取消 ✓，
  所以"已提交"必须从历史读 ✓。
- 图的默认 agent（mini）**不产生步骤记录** ✓，也没有接缝把"节点已提交"告诉图 ✓——A6 的受阻根因 ✓。
- `subprocess` 的清理需要**不关闭管道**地等待读取线程 ✓——关掉会丢掉缓冲数据 ✓（实测：三个测试轮流失败 ✓）。

**未验证**：真实 provider 的窗口拒绝、摘要质量与引用真实性、断电耐久性 ✓（SIGKILL 不等价 ✓）、
多文件事务性 ✓（rename 的原子可见性不等价 ✓）、`B2/B3` 与 `B4–B6`、`B8` 的完整闭环 ✓。

## 7. 一条命令跑完

```bash
.venv/bin/python scripts/recovery_windows.py                     # C1–C9 + A1 + A3 + A4 + A5 + A6
.venv/bin/python scripts/recovery_windows.py --only A1           # 单个案例
.venv/bin/python scripts/recovery_windows.py --json evidence.json
.venv/bin/python -m pytest tests/test_node_recovery.py tests/test_node_integration.py -q
.venv/bin/python -m pytest tests/ -q
.venv/bin/ruff check src/ tests/ scripts/
.venv/bin/mypy src/anchor/
```

**实测**：本包 29 项 ✓（21 + 8 ✓）；全量 `tests/` **225 项通过** ✓、**无 skip** ✓；`ruff` ✓ 与 `mypy`（25 个源文件）✓ 通过 ✓。
故障脚本的**失败或受阻返回非零** ✓，每个窗口有界超时 ✓，`finally` 只清理自己记录过的进程 ✓。

**未使用的私有 API**：无 ✓。测试注入用的接缝是图自己的 `_agent_for` ✓（并在代码里注明这是故障测试设施 ✓）。
