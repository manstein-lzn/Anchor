# G2 实施结果（第三次交付）：R1–R5 修复与 B1–B8 执行证据

基线：分支 `research/node-g2`，HEAD `575586f` ✓。**本文件不是 G2 通过的声明** ✓——主验收独立复核后写
`AGENT_NODE_VALIDATION_RESULT.md` ✓。

环境：Python 3.12.3 ✓ · Linux 7.0.0-31-generic x86_64 ✓ · bubblewrap 0.9.0 ✓ ·
`pydantic-ai-slim==2.46.0` ✓ · `pydantic-ai-harness==0.32.0` ✓ · `mini-swe-agent==2.4.6` ✓ ·
`litellm==1.101.0` ✓ · `pydantic==2.13.5` ✓。**无付费 provider 调用、无凭证读取** ✓。

## 0. 状态总表

| 项 | 状态 | 证据 |
| --- | --- | --- |
| **R1** 完成协议 | ✅ 方向已修 | 结构化完成事实 ✓；假提交不再当完成 ✓；6 类反例 ✓ |
| **R2** 预算耗尽 | ✅ 方向已修 | 8/8 → **0 请求** ✓、额度不被提高 ✓、记账失败即停 ✓ |
| **R3** 逐流预览 | ✅ 已修 | 两流各 8000 全可见 ✓、零丢失 ✓ |
| **R4** B1/B7 夹具 | ✅ 已修 | B1 由**阶段证据**驱动 ✓；B7 真开持久化 ✓ |
| **R5** 图级对象 | ✅ 已换成候选 Node + 真图 | 见 §2 ✓ |
| **B1** 压缩后恢复 | ✅ **通过** | 真压缩在 kill 之前 ✓、输入有界 ✓、约束存活 ✓、无重复步骤 ✓ |
| **B2** 压缩两侧中断 | ✅ **通过**（3 窗口）| 压缩前/压缩后/检查点后 ✓，选中的历史**自洽** ✓ |
| **B3** 副作用间隙（压缩后）| ✅ **通过**（3 窗口）| 三个间隙全部 `uncertain` ✓ |
| **B4** 输出引用 | ✅ **通过**（2 窗口）| 恢复进程读回尾部 ✓、写入被拒 ✓、被裁明说 ✓ |
| **B5** 预算组合 | ❌ **未通过** | 两类各自计数 ✓，而持久化额度与它们的和**不相等** ✗（见 §3 ✓）|
| **B6** 原始记录关联 | ✅ **通过** | 只增不减 ✓、各类原始事实都在 ✓、trace 不代替它 ✓ |
| **B7** 完成控制流 | ✅ **通过** | 真同时开持久化+上下文 ✓、引用交回 **0 请求** ✓ |
| **B8** 图闭环 | ❌ **未通过** | resume 不看节点记录 → 工作**做了两遍** ✗（见 §4 ✓）|

**未通过的两项如实标为未通过** ✗——**不**用测试数量抵消 ✓（验收 §68 ✓）。

## 1. 上一轮三条阻塞的处理

| 阻塞 | 处理 |
| --- | --- |
| **① 组合矩阵未完成** | B1–B8 全部执行 ✓；B5、B8 **未通过** ✗，B2/B3/B4/B6/B7 通过 ✓ |
| **② B7 代码证据与报告不一致** | **属实** ✓——文件里有**两个** B7 ✓，旧的那个只传 `context_capabilities` ✓、注释还写着"B7 的一半" ✗ ✓。旧的已删 ✓，留下的那个**补了真恢复阶段** ✓（新进程 ✓、引用 ✓、**0 请求** ✓）|
| **③ 图级恢复受阻** | R5 的对象已换成**候选 Node + 真图** ✓；B8 把结论推到底 ✓——见 §4 ✓ |
| **④ 摘要路径需先定行为** | **顺序已定** ✓：摘要器**在前** ✓、窗口是兜底 ✓。原来窗口在前 ✗，后果是**摘要器从不运行** ✗（一条完整运行的记录里 4 次压缩**全是**滑动窗口 ✓）。改后 B1 的记录显示 `SummarizingCompaction` ✓ 与 **13 次真摘要调用** ✓。**口径未定** ✗——见 §3 ✓ |
| **⑤ 共享层需主集成复核** | R3 的修复有确定性测试 ✓；**恢复后的挂载**由 B4 覆盖 ✓（真沙箱内读到尾部 ✓、写入被拒 ✓）；输出生命周期与清理由 §5 覆盖 ✓ |

## 2. R5 / B8：图级的真实状态

**候选 Node 真的在真图里跑** ✓（三节点：agent → op → agent ✓，只把 agent 那一步经 `run_node` ✓）：

```
B8  killed=True barrier="before finish: the graph's earlier passes are recorded"
    passes recorded ['count','finish','write']; note.md 1, size.txt 1 → 恢复后 note.md 2, size.txt 2
```

**R5 的结论收窄为**：窗口不是"不存在接缝" ✗，而是**接缝存在、而 `resume=` 不用它** ✓。已加的最小接缝是
`run(..., already_submitted=…)` ✓——调度器在**跑节点之前**问"这个节点的记录说它已经提交了吗" ✓，并按答复
记录这一趟 ✓ ✓。节点层的半边**成立** ✓（事实在 ✓、读回精确 ✓、**0 请求** ✓）；而 `resume=` 走的是图自己的
状态 ✓（它知道**自己记过哪些 pass** ✓，不知道某个它还没记的节点内部发生了什么 ✓）。

## 3. B5：两类计数都对，口径不对

三类调用**各自都被独立计数** ✓（节点自己的在一个日志 ✓、摘要的在另一个 ✓，都写在请求真正到达的地方 ✓）。
而**持久化额度与它们的和不相等** ✗，且**偏差方向不一致** ✗：

| 场景 | 节点调用 | 摘要调用 | 和 | 持久化 |
| --- | --- | --- | --- | --- |
| 完整流程（多次 kill 与恢复）| 13 | 9 | **22** | **13** ✗（少记）|
| 同接线的直接测量 | 12 | 7 | **19** | **24** ✗（多记）|

所以问题不是"漏了一类" ✗，而是**口径未定义** ✗：`charge` 接缝存在且被调用 ✓，而与适配器按请求记的账
**互相覆盖** ✓。验收第 4 条要求的正是"明确策略顺序和摘要预算口径" ✓——**顺序已定** ✓，**口径未定** ✗。

## 4. §5 组合矩阵逐项证据（可从 `--json` 取回）

| 窗口 | 判定 | 要点 |
| --- | --- | --- |
| `B1` | `compacted-then-resumed` | kill 前 **2 次真压缩** ✓、输入 3343→**1563 稳定** ✓、约束 **17/17** ✓、无重复步骤 ✓ |
| `B2-before-compaction` | `consistent` | **0 次压缩** ✓、什么都没跑 ✓（对照 ✓）|
| `B2-after-compaction` | `consistent` | 1 次压缩 ✓、历史**自洽** ✓、`uncertain` ✓ |
| `B2-checkpoint` | `consistent` | 3 次压缩 ✓、`continuable` ✓、自洽 ✓ |
| `B3-started` / `B3-effect` / `B3-terminal` | `consistent` ×3 | 压缩后三个间隙全部 `uncertain` ✓ |
| `B4` | `readable-and-read-only` | 300 KB 全在 ✓、恢复进程**读回唯一尾标记** ✓、写入被拒 ✓ |
| `B4-partial` | `cut-and-said` | store 放不下时模型**被告知**是片段 ✓（由模型自证 ✓）|
| `B5` | `BAD` | 见 §3 ✗ |
| `B6` | `append-only-and-correlated` | 20 430→86 320 字节只增 ✓、`initial_context`/原始响应/命令全文/`tool_call_id`/摘要/压缩全在 ✓、trace 是**另一个文件** ✓ |
| `B7` | ✅ | 提交后命令不执行 ✓、1 次请求 ✓、引用交回 **0 请求** ✓ |
| `B8` | `BAD` | 见 §2 ✗ |

**B2/B3 的一条结论值得记下** ✓：store 里的历史是 **settle 后的那一份** ✓，压缩是**每次请求派生**的 ✓——所以
摘要**不可能**被拼到不是它来源的历史上 ✓：两者在 store 里从不相遇 ✓。而"自洽"是**直接读消息**数出来的 ✓
（每个 `tool-call` 都有配对的 `tool-return` ✓）。

## 5. 共享执行层（§4）

| 检查 | 状态 |
| --- | --- |
| 预览与共享存储额度是**两个**限制 | ✅ 40 KB/10 KB/1 MB ✓ |
| **预览上限每流各一份**（R3）| ✅ 三类边界 ✓ |
| 运行期间内存与磁盘有界 | ✅ **运行期间采样** ✓ + 采样次数校验 ✓ |
| 无存储不交出已删除路径 | ✅ ✓ |
| 超时有限截止、暂存清空 | ✅ ✓ |
| **恢复后只读挂载真的可读、且写不进** | ✅ **B4**：真沙箱内读到尾部 ✓、写入被拒 ✓ |
| 节点写不进审计记录与存储 | ✅ 记录在控制目录 ✓、挂载只读 ✓ |

## 6. 交付物与命令

```bash
# 全部 26 个窗口，机器可读证据
.venv/bin/python scripts/recovery_windows.py --json evidence.json

# 单个案例
.venv/bin/python scripts/recovery_windows.py --only B5

# 测试与静态检查
.venv/bin/python -m pytest tests/ -q
.venv/bin/ruff check src/ tests/ scripts/
.venv/bin/mypy src/anchor/
```

**实测**：本包 **37 项** ✓；全量 `tests/` **233 项通过** ✓、**无 skip** ✓；`ruff` ✓ 与 `mypy`（25 个源文件）✓
通过 ✓。每个 `--json` 条目含窗口名 ✓、控制目录与 workspace ✓、屏障文本 ✓、退出码 ✓、counter ✓、判定与理由 ✓、
预算 ✓。

**本轮 commit**：`12938b2`(顺序与旧 B7) ✓ · `ba56d63`(B2/B3) ✓ · `b1a8688`(B4) ✓ · `ae7bd84`(B5) ✓ ·
`575586f`(B8) ✓。

## 7. 未验证、不声称与待办

- **不声称 exactly-once** ✓、**不声称生产级恢复** ✓。
- **B5、B8 未通过** ✗——各自的实测理由在 §2/§3 ✓，**不是**"未执行" ✓。
- **`run` 有带说明的 `# noqa: C901`** ✓：加了 `already_submitted` 之后分支数为 17 ✓，我抽出了三个助手 ✓
  （`_asked_to_stop`/`_cease`/`_settled_already` ✓）而**条件数未减少** ✓——`run` 是调度器 ✓，条件就是它的形状 ✓。
  这是**刻意的例外** ✓，写在这里而不是藏在代码里 ✓。
- **共享层改动**（`sandbox.py` ✓、`execenv.py` ✓、`context.py` ✓）请主集成复核 ✓。
- **未验证**：真实 provider 的窗口拒绝 ✓、摘要质量与引用真实性 ✓、断电耐久性 ✓、多文件事务性 ✓。
