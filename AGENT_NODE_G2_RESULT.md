# G2 实施结果（第二次交付）：R1–R5 修复与状态更正

基线：分支 `research/node-g2`，HEAD `34e1422` ✓。**本文件不是 G2 通过的声明** ✓——主验收独立复核后写
`AGENT_NODE_VALIDATION_RESULT.md` ✓。

环境：Python 3.12.3 ✓ · Linux 7.0.0-31-generic x86_64 ✓ · bubblewrap 0.9.0 ✓ ·
`pydantic-ai-slim==2.46.0` ✓ · `pydantic-ai-harness==0.32.0` ✓ · `mini-swe-agent==2.4.6` ✓ ·
`litellm==1.101.0` ✓ · `pydantic==2.13.5` ✓。**无付费 provider 调用、无凭证读取** ✓。

## 0. 撤回上一版首页的两条结论

> **上一版说**：「节点层安全与可用底线都立得住，唯一立不住的是图的 finalize 那一步。」
> **这两条都撤回。** ✗

| 撤回的结论 | 为什么错 |
| --- | --- |
| **「节点层的安全底线成立」** ✗ | R1 的真实反例：一条打印完成标记、随后 `exit 1` 的命令，**正常路径明确拒绝** ✓，而恢复把它当成 `finished` ✓，还把拒绝说明当作提交文本交回 ✓——协议有两个实现，第二个是错的 ✓ |
| **「节点层的可用性底线成立」** ✗ | R2 的真实反例：额度 **8/8** 时，新进程经真实恢复入口**仍然发出 1 次请求** ✓，结果 `completed` ✓，预算文件变成 **9/8** ✓ |
| **「唯一阻塞是图 finalize」** ✗ | A6 用的是 **mini 默认路径** ✓——那条路**没有步骤 store** ✓——所以"store 里没有 run"只说明那条路没接入候选持久化 ✓，**不能**当作候选架构的结论 ✓（第一次交付自己也承认了这一点，却仍把它当作结论 ✓）|
| **「共享层检查通过」** ✗ | R3 的真实反例：`max_output_bytes=10000`、两流各 8000 字节，stdout 只剩 **2000** ✓、`incomplete=False` ✓、**6000 字节静默消失** ✓ |

**逐项的真实状态见 §1–§3** ✓。测试数量**不能**抵消执行证据 ✓（验收 §68 ✓）。

## 1. R1–R5：反例、修复与证据

全部为**真实执行** ✓：真 `SIGKILL` ✓、真 `bubblewrap` ✓、真 CLI ✓、真 Git ✓、恢复是**新的操作系统进程** ✓；
kill 时刻由**同步屏障**决定 ✓（节点到达窗口 → 写一个字节到管道 → 阻塞 ✓；父进程读到**那个字节**才 kill ✓，
`exit_code == -9` 与屏障文本一起进证据 ✓）。

| ID | 反例（复现过） | 修复 | 现测 |
| --- | --- | --- | --- |
| **R1** 完成协议 | 打印标记后 `exit 1` ✓ → 恢复报 `finished` ✓ 且提交里带着拒绝说明 ✓ | **在协议说"是"的地方写结构化事实** ✓（`bash` 工具内 ✓，落在控制目录 ✓）；恢复读事实 ✓，不再扫文本 ✓ | ✅ 假提交 → 不 `finished` 且无事实 ✓；真提交 → `finished` ✓ 且提交**逐字**一致 ✓；非零退出/超时/标记在中间/非法 route/多出口误用 `done` 全部按协议拒绝 ✓ |
| **R2** 预算 | 8/8 → 模型端 **+1** 次调用 ✓，`completed` ✓，预算 **9/8** ✓ | 三处成因都去掉 ✓：`remaining=0` 不再被换成新额度 ✓；不再保底 1 次 ✓；`allowed` 取**更小**者 ✓；**记账失败即抛**（不再吞掉 ✓）；只带引用也记账 ✓ | ✅ 8/8 → **0 次请求** ✓、`budget_exhausted` ✓、预算仍是 **8/8** ✓；A5 六次真 kill 逐轮相符并到耗尽 ✓ |
| **R3** 双流 | 两流各 8000、上限 10000 → stdout **2000** ✓、`incomplete=False` ✓、静默丢 6000 ✓ | 预览**每流各一份** ✓，共享的只有存储预算 ✓（`keep = max(limit, budget)` 那个两流共用的上限去掉了 ✓） | ✅ 两流各 8000 **全可见** ✓、零丢失 ✓；长流+短流 → 预览+截断提示 ✓、`incomplete=True` ✓ |
| **R4** B1/B7 | B1 在 kill 前**没有压缩** ✓、恢复后**重做第一步** ✓、压缩发生在恢复**之后** ✓；B7 只传了上下文能力 ✓，没有持久化与恢复 ✓ | 模型替身改由**阶段证据**驱动 ✓（读历史里最大的 `STEP-n` ✓）；屏障等记录里出现真压缩再放一个已 settle 的周期 ✓；B7 真开 `StepPersistence` + `recovery_store` ✓ | ✅ B1：kill 前 **2 次真压缩** ✓、输入从 3343 字符降到 **1445 并稳定** ✓、约束 **17/17** 次都在 ✓、恢复进程 `completed` ✓、**无步骤重复** ✓；B7：提交后命令不执行 ✓、1 次请求 ✓、事实已记 ✓、引用交回结果 **0 次请求** ✓ |
| **R5** 图级 | 用 mini 路径 ✗ | 三节点真图（agent→op→agent ✓），**只把 agent 那一步**接到 `run_node` ✓ | ⚠️ **受阻，但换成了正确的对象与证据** ✓——见下 |

### 1.1 R5 的正确结论：窗口本身够不到

用候选 Node 跑真图之后 ✓：

```
killed=True exit=-9 barrier='submission persisted, the graph has not finalised'
  the **candidate** node left 1 run(s) in the step store          ← 候选 Node 真在真图里跑了
  assessing one says 'finished'                                    ← 完成是记录下来的事实
  handing the reference back returns submission 'wrote it' with 0 model request(s)   ← 节点层可以
  by the time the kill landed the graph had recorded ['count','finish','write']
```

**一个提交的节点不会再有模型请求** ✓——所以最后一个 agent 侧钩子在提交**之前** ✓，下一个钩子属于**下一个
节点** ✓——`agent.run(task=...)` 返回到 `_record(state, graph, run_dir, decided, result, settle)`
（`src/anchor/simple/run.py` ✓）之间**一个钩子都没有** ✓。

**这是交回的最小接口缺口** ✓：节点层恢复成立 ✓（精确提交 ✓、零请求 ✓），而图的那一半**需要一个接缝**
才能被告知"这个节点已经提交" ✓。**不是**"图不存在可用接缝"的断言 ✓，也不是用普通命令代替提交 ✗。

## 2. A 门槛逐项状态

| ID | 状态 | 证据 |
| --- | --- | --- |
| **A1 实际继续** | ✅ 通过 | 进程一完成后被杀 ✓；进程二只拿引用 ✓，用同一执行实现继续到有效提交 ✓；`counter` 仍是 **1** ✓ |
| **A2 写入间隙** | ✅ 通过 | 源码：`after_tool_execute`（`:622`）与 `after_node_run`（`:661`）**两次独立写入** ✓；kill 落在中间 ✓；**旧 complete 快照不覆盖已 settle 的副作用 → `uncertain`** ✓ |
| **A3 重复恢复** | ✅ 通过 | 真实入口顺序三次 ✓；`uncertain`→0 请求 ✓；`continuable`→提交 ✓；`finished`→**0 请求**且提交一致 ✓ |
| **A4 引用与损坏** | ✅ 通过 | 错 store/错 node/未知 run/版本/负预算/类型 + **真实损坏的 run·快照·events·预算** ✓——全部有解释地拒绝 ✓、**零命令执行** ✓；引用预算不能覆盖目录里更高的已用额度 ✓ |
| **A5 真预算累计** | ✅ 通过（**已重做**）| **六次真 kill + 重启** ✓，逐轮两份独立账目相符 ✓（1/6=1 … 6/6=6 ✓），额度不被提高 ✓，耗尽后**一次请求都没有** ✓ |
| **A6 提交与图完成间隙** | ⚠️ **受阻（含最小接口缺口与源码位置）** | 见 §1.1 ✓ |

## 3. §4 与 §5 的组合矩阵

**§4 共享执行层**（`tests/test_node_integration.py` ✓，真实执行 ✓）：

| 检查 | 状态 |
| --- | --- |
| 预览与共享存储额度是**两个**限制 | ✅ 40 KB 输出 / 10 KB 预览 / 1 MB 预算：预览有界 ✓、完整输出在盘上 ✓ |
| **预览上限是每流各一份**（R3 ✓）| ✅ 三类边界都测了 ✓ |
| 运行期间内存与磁盘有界 | ✅ **运行期间采样**（原来只看结束后的目录大小 ✓ 已修正 ✓），并检查采样次数不为 0 ✓ |
| 无存储配置不交出已删除路径 | ✅ 不交路径 ✓、明说没能完整保留 ✓ |
| 超时有限截止、暂存清空 | ✅ 1 秒超时在有限时间内返回 ✓、超时前输出保留 ✓、暂存为空 ✓ |

**§5 组合矩阵**：

| ID | 状态 |
| --- | --- |
| **B1 压缩后恢复** | ✅ **通过（已重做）**——真压缩在 kill **之前** ✓、检查点在其后 ✓、新进程继续 ✓、输入有界 ✓、约束存活 ✓、工具不重复 ✓ |
| **B2 压缩两侧中断** | ❌ **未执行** |
| **B3 副作用间隙（压缩后）** | ❌ **未执行** |
| **B4 输出引用（恢复进程中读回）** | ❌ **未执行** |
| **B5 预算组合（摘要与溢出重试跨恢复）** | ❌ **未执行**——本轮**发现**摘要器根本不运行 ✓（见 §5 ✓），需要先定这件事 ✓ |
| **B6 原始记录跨多次压缩与恢复的关联** | ❌ **未执行** |
| **B7 完成控制流** | ✅ **通过（已重做）**——真同时开持久化与上下文 ✓ |
| **B8 图闭环** | ❌ **未执行**——A6 已把图级对象换对了 ✓，但"压缩后中断再恢复到图完成"还没跑 ✓ |

**未执行的如实标为未执行** ✗——不用测试数量替代 ✓（§68 ✓）。

## 4. API 改动、共享文件与 commit

| 文件 | 改动 |
| --- | --- |
| `src/anchor/node/__init__.py` | `UNCERTAIN` 状态 ✓、`NodeRequest.recovery` ✓、`NodeOutcome.recovery` ✓ |
| `src/anchor/node/pydantic_adapter.py` | `_what_a_previous_attempt_left` ✓、`run_node(..., recovery_store=)` ✓、**完成事实的写入** ✓、`_CountingModel` 的口径统一 ✓、引用绑定检查 ✓ |
| `src/anchor/node/recovery.py` | `CompletionFact` 的读写 ✓、`finished` 判定 ✓、快照覆盖检查 ✓、绑定检查与 `verify` ✓、预算校验/取高者 ✓、损坏转拒绝 ✓ |
| `src/anchor/runtime/sandbox.py` | **R3 的共享层修复** ✓（每流预览 ✓）——**这是共享文件，请主集成复核** ✓ |
| `scripts/recovery_windows.py` | 单命令入口 ✓：C1–C9 + A1 + A3 + A4 + A5 + A6 + B1 ✓，`--json` 机器可读证据 ✓ |
| `tests/test_node_recovery.py` · `tests/test_node_integration.py` | 23 + 10 项 ✓ |

**本轮 commit**：`c9f6a9a`(R1) ✓ · `ef25b13`(R2) ✓ · `960dbfa`(R3) ✓ · `b0eb86d`(R4) ✓ · `6079a55`(R5) ✓ ·
`34e1422`(引用绑定) ✓。

## 5. 框架与包 2 的限制（本轮新发现）

1. **一个提交的节点不会再有模型请求** ✓——所以"提交已持久化、图还没记"那一瞬**不在任何 agent 钩子上** ✓。
   这是 A6 受阻的根因 ✓，也是图级衔接需要的最小接缝 ✓。
2. **`_compact_once` 停在第一个真正减少历史的策略** ✓，而列表里 `SlidingWindowCompaction` **在前** ✓——
   所以**摘要器在这个配置下从不运行** ✓（B1 的记录里策略只有滑动窗口 ✓）。这与包 2 的说明
   "先摘要后丢弃"**不一致** ✓，建议包 2 复核 ✓；B5 也需要先定这件事 ✓。
3. **钩子方向不同** ✓：`before_*` 按注册顺序 ✓、`after_*` 按逆序 ✓——决定一个钩子落在写之前还是之后 ✓。
   C1 因此可达 ✓（计划期望的 `replayable` ✓）。
4. **`run_id` 单次有效** ✓——恢复是新的 `Agent.run` ✓，逻辑序列走 `conversation_id` ✓。
5. 成功路径上**没有** `run_completed` ✓（本节点在提交后的边界离开 `agent.iter` ✓），所以"已提交"只能从
   记录读 ✓。

## 6. 一条命令与机器可读证据

```bash
# 全部故障窗口（C1–C9 + A1 + A3 + A4 + A5 + A6 + B1），带机器可读证据
.venv/bin/python scripts/recovery_windows.py --json evidence.json

# 单个案例
.venv/bin/python scripts/recovery_windows.py --only A5

# 测试与静态检查
.venv/bin/python -m pytest tests/ -q
.venv/bin/ruff check src/ tests/ scripts/
.venv/bin/mypy src/anchor/
```

每个 `--json` 条目含：窗口名 ✓、控制目录与 workspace 路径 ✓、屏障文本 ✓、kill 与恢复进程退出码 ✓、
counter 前后值 ✓、判定与理由 ✓、预算轨迹 ✓、以及该案例自己的说明 ✓。

**实测**：本包 **33 项** ✓（23 + 10 ✓）；全量 `tests/` **229 项通过** ✓、**无 skip** ✓；
`ruff` ✓ 与 `mypy`（25 个源文件）✓ 通过 ✓。**没有使用私有 API 作为生产依赖** ✓。

## 7. 未验证与不声称

- **不声称 exactly-once** ✓，**不声称生产级恢复** ✓。
- **未验证**：真实 provider 的窗口拒绝 ✓、摘要质量与引用真实性 ✓、断电耐久性 ✓（SIGKILL 不等价 ✓）、
  多文件事务性 ✓（rename 的原子可见性不等价 ✓）。
- **未执行**：B2 ✓、B3 ✓、B4 ✓、B5 ✓、B6 ✓、B8 ✓（见 §3 ✓）。
- **R5 的图级衔接受阻** ✓，已附最小接口缺口与源码位置 ✓。
- **共享层改动**（`sandbox.py` ✓、`execenv.py` ✓）**待主集成复核** ✓。
